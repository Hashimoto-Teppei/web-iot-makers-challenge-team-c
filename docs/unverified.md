# 実機で未確認のこと

開発速度を優先し、**実機での確認はスキップして進める**方針を取っている（`docs/adr/0002-development-lifecycle.md`）。
ただし何を確かめていないかを見失うとデモ直前に詰むため、**未確認の前提はここに積む**。

PR で実機未確認の前提を置いたら、この表に1行足すこと。確認できたら行を消す（履歴は git に残る）。

**番号は追加した順で、欠番はそのままにする。** 詰めると他のファイルからの参照がずれる。

| # | 未確認の前提 | 影響範囲 | 確かめる方法 | 起票 |
| --- | --- | --- | --- | --- |
| 3 | Zero 2 W のリソースで BLE の常時接続・検知・表示制御を同時に回せる（**RAM 512MB。CPU は4コア**） | デバイス全体 | 実機 | 2026-08-23 |
| 4 | BLE でデバイスからモバイルアプリへログを転送できる | デバイス / モバイル | 実機 + Android 端末 | 2026-08-23 |
| 5 | 検知のしきい値が実走行で妥当（誤検知・見逃しの頻度） | 全検知 | 実機 + 実走行 | 2026-08-23 |
| 7 | 想定する部品をすべて足しても、電源が走行中もつ（消費電流の見積もりが妥当） | デバイス全体 | 実機 + 電流計 | 2026-08-24 |
| 8 | **測位の更新頻度 1Hz で検知が成立する。**足りなければスマホ側で上げられる | 全検知 | 実機 + 走行 | 2026-08-24 |
| 9 | スマホの水平精度で、狙った標識と隣の道路の標識を区別できる | 標識の検知（#27） | 実機 + 現地 | 2026-08-24 |
| 11 | MTU 247 で流しっぱなしにして、1回の走行分のログを待てる時間内に転送しきれる（ラズパイが送信に追いつく）。**`adr/0006` で運ぶのが検知ログだけになり、量は1桁減った**ので、当初より通りやすい | デバイス / モバイル | 実機 + Android 端末 | 2026-08-25 |
| 13 | Android の `requestMtu(247)` がラズパイとの間で **163 以上**にネゴシエートできる（`alert` の1通の上限 160 バイト + ATT の 3 バイト。**64 では足りない**）。**アプリ側は動いた**——Pixel 10（Android 17）から macOS の疑似ペリフェラル相手に **517** が返り、下回れば接続を諦める（`apps/mobile/src/ble/link.ts`）。**残っているのはラズパイ相手の値だけ** | デバイス / モバイル | 実機 + Android 端末 | 2026-08-26 |
| 14 | **Notify を流しっぱなしにしても取りこぼしが起きない**（アプリ層の再送を置かない前提。`docs/interfaces/ble-log-transfer.md`「転送の約束」） | デバイス / モバイル | 実機 + Android 端末 | 2026-08-26 |
| 15 | **確認できた**（2026-09-19）。ラズパイの BlueZ が **5.62 以降**で、`GattCharacteristic1` の `MTU` プロパティが読める（読めないと Notify を何バイトで切ればよいか分からない）。**実機は 5.82**（Debian 13 trixie）。**次に消すときにこの行ごと落とす** | デバイス | 実機で `bluetoothctl --version`（**済**） | 2026-08-26 → 2026-09-19 |
| 21 | **スマホの測位精度が検知に耐える。**ハンドルにマウントした状態で `Location.getAccuracy()` が実際の誤差とかけ離れていない | 全検知 | 実機 + 屋外（既知の地点との差を見る） | 2026-08-26 |
| 22 | **Android のフォアグラウンドサービスが走行中ずっと常駐し続ける。**一部メーカーの省電力機構は、規格どおりに書いても停止させる。**Pixel 10（Android 17）で 9 分は通った**（画面を消したまま `alert` の心拍が 1Hz で途切れず届き続けた。種別 `location` のサービス。2026-09-03）。**1時間と、デモに使う端末での確認が残っている** | 全検知 | **Android の予備機**で1時間以上の連続稼働（**デモに使う端末は iPhone に移った → 95**。`docs/adr/0010-ios-primary-target.md`） | 2026-08-26 |
| 23 | **スマホ ⇄ Worker の往復遅延が、混んだ会場でも検知に耐える。**HTTPS は TCP なので、詰まると古い位置がまとめて届く | 車車間の位置共有 / 全検知 | 実機 + 会場相当の電波環境 | 2026-08-26 |
| 24 | **欠番。**`uplink` で毎秒何通通るかを測る前提だったが、`docs/adr/0006-decision-layer-on-mobile.md` で**位置を BLE で運ばなくなり、前提ごと消えた** | — | — | 2026-08-26 |
| 25 | **表示までは確認できた**（2026-09-19）。**判定が発火し、それが人に見えるところまで通った** ——`link が nofix → down に変わった`（心拍が 3 秒来なかった）と屋内での `nofix` がラズパイのログに出て、**30 秒後に `disconnect_central()` まで走り**、**LCD の下段に `DOWN  >` が出ているのを目で確かめた**。**残っているのは LED 側だけ**——まだ挿さっていないので、光の方は journalctl の中にしかない（→ #151） | 全検知（安全） | 実機 + 部品（**LED 2個を挿す回に見る**） | 2026-08-26 → 2026-09-19 |
| 26 | **物体検知センサーが屋外・走行中に実用になる。**振動・風の影響と、実際の検知距離。**デバイスに残る唯一の検知になった**ので、重みが上がった。**部品は AE-NJR4265 J1 に決まった**（`docs/hardware.md`）ので、**日光の影響は消えた**（マイクロ波なので）。代わりに**±35°の検知角で、隣の車線・路側の壁・ガードレールを拾わないか**が残る | ローカル検知（#26） | 実機 + 走行（**92 と一緒に見る**） | 2026-08-26 |
| 27 | **経路の取りこぼしが、近傍の失効（既定 3 秒 ＝ 3回連続の欠測）に耐える。**BLE と LTE のどちらが詰まっても穴が開く | 車車間の位置共有 / 全検知 | 実機2台以上 + 会場相当の電波環境 | 2026-08-26 |
| 28 | **新しい部品の信号線が 3.3V である**（5V ならラズパイが壊れる）。基板に 3.3V レギュレータが載っていても保証されない | デバイス全体 | **配線する前に**テスターで測る | 2026-08-26 |
| 29 | **時刻に関わるしきい値の既定値が実走行で妥当**（近傍の失効、**サーバー側の失効との合計**、`peer` の `t` の上下限、通信断と判定するまでの時間）。**狭すぎると健全な走行中に検知が無音になり、広すぎると過ぎ去った位置で警告が鳴る** | 車車間の位置共有 / 全検知 | 実機2台以上 + 走行 | 2026-08-26 |
| 30 | **1Hz の HTTP POST が、Android の電池と LTE で走行時間ぶん持続する。**コネクションが再利用されないと毎回 TLS ハンドシェイクが走り、電池と遅延の両方が悪化する | モバイル ⇄ API / 全検知 | 実機 + 屋外で1時間以上 | 2026-08-29 |
| 31 | **半径 300m の中に多数の自転車がいても、スマホ側が1秒の周期で処理しきる。**発表会場では全員が互いの半径に入るため、`peer` が数十台になる。**BLE の飽和は前提ごと消えたが**（`docs/adr/0006-decision-layer-on-mobile.md`）、**JSON の大きさと検知の計算量は残っている** | 車車間の位置共有 / 全検知 | 実機 + 端末を複数 | 2026-08-29 |
| 32 | **スマホが3つの検知と一時停止の事前通知を毎秒回しても、測位・通信・心拍が遅れない。**判断がスマホへ移ったことで新しく生まれた前提（`docs/adr/0006-decision-layer-on-mobile.md`） | 全検知 | 実機 + 走行（**22・30 と一緒に測る**） | 2026-08-31 |
| 34 | **1Hz の測位で「止まった」が判定できる。**測位が毎秒1点なので、**1秒より短い停止は点として現れない**（`docs/adr/0007-keep-raw-ride-logs.md`） | 不停止の集計 | 走行ログを取り、実際に止まった地点と突き合わせる | 2026-08-31 |
| 35 | **セルの大きさ（小数3桁 ≒ 110m）と通過回数の下限が、実データで妥当。**細かすぎれば同じ交差点が隣のセルに割れ、粗すぎれば交差点と手前の直線が混ざる | 集計 / 画面 | 実走行のログで試す | 2026-08-31 |
| 36 | **履歴を 5 秒ぶん保てば、減速度と接近速度が実用の精度で出せる。**1Hz なら5点しか無く（8）、短すぎれば測位のゆらぎを減速と読み違え、長すぎれば古い点を含んだ平均が鈍って**発火が遅れる** | 全検知（`docs/interfaces/detectors.md`） | 走行ログを取り、実際の加減速と突き合わせる | 2026-08-31 |
| 37 | **シミュレータで確認できたことが、実走行でもそのまま成立する。**合成データには測位のゆらぎが入っていないので、**ここで出ない誤検知と見逃しがある**（`apps/mobile/src/sim/`） | 全検知 / シミュレータ | 実機2台 + 走行（**5 と一緒に見る**） | 2026-08-31 |
| 38 | **2点の位置の差から出した接近速度が、測位のゆらぎに耐える**（`apps/mobile/src/detect/approach.ts`）。水平精度が 4m でも、2秒の窓では**止まっている相手が 2 m/s で迫って見えうる**——発火のしきい値（既定 2.0 m/s）と同じ大きさである。**足切りは既定 8m（`maxHaccM`）に置いたが、窓を延ばす方が効くかもしれない——実データを見て決める** | 急接近の検知（#9） | 走行ログを取り、静止した相手との相対速度を見る | 2026-08-31 |
| 39 | **相手の向きを見ずに距離の変化だけで判断して、すれ違う対向車が誤検知にならない**（`apps/mobile/src/detect/approach.ts`）。**向きで絞ると、進路上に止まっている自転車を落とす**ので絞っていないが、対向 5 m/s 同士なら接近速度は 10 m/s になり、**すれ違うたびに鳴る可能性がある** | 急接近の検知（#9） | 実機2台 + 走行（**5 と一緒に見る**） | 2026-08-31 |
| 40 | **決着した**（2026-09-19）。実機は **Raspberry Pi Zero 2 W Rev 1.0 / aarch64 / Debian 13 (trixie) / Python 3.13.5**（CHIRIMEN Lite）で、**Zero W でも 32-bit でも Bookworm でもなかった。****`requires-python` を `>=3.13,<3.14` に寄せた**ので、**システム Python がそのまま使える**（`docs/adr/0001-tech-stack.md` / `0008-device-dependencies.md`）。**残っているのは、その上で依存が入るかどうかだけ** → 41 / 42 | デバイス全体 | **済** | 2026-08-31 → 2026-09-19 |
| 41 | **確認できた**（2026-09-19）。**aarch64 / cp313 でも `PyGObject` と `pycairo` はソースビルドに落ちる**が、**apt の開発パッケージを入れれば数分で通る**（`docs/deploy-device.md` の 3）。**「piwheels が要らなくなった＝ホイールが降ってくる」ではなかった** ——それでも**方針は変わらない。** piwheels に頼っていた理由は**「ARMv6 ではビルドが事実上終わらない」**ことで、**4コアの Zero 2 W ではその理由ごと消えている**（`docs/adr/0008-device-dependencies.md`）。**次に消すときにこの行ごと落とす** | デバイス全体 | **済**（`uv sync --group device --python /usr/bin/python3`） | 2026-08-31 → 2026-09-19 |
| 42 | **半分外れた**（2026-09-19）。`import bluezero` は通ったが、**`bluezero.adapter` が `dbus`（`dbus-python`）を import して落ちる** ——`ModuleNotFoundError: No module named 'dbus'`。**`bluezero` の `dependencies` に入っていない**ためで、**`import bluezero` だけでは確かめられない**（**あれは何も import しない空の `__init__.py` である**）。`libgirepository1.0-dev` を入れる必要があったことも含め、**「最初から入っている」は成り立たなかった。****`dbus-python` を足したら `device.main` は起動し、`GATT application registered` まで通った**（2026-09-19）。**次に消すときにこの行ごと落とす** | デバイス全体 | **済**（`uv run python -m device.main`。`import bluezero` では足りない） | 2026-08-31 → 2026-09-19 |
| 43 | **半分確認できた**（2026-09-20）。**`bluetooth` グループは要らなかった** ——`id -nG` にその名前は無いまま（`pi` に付いているのは `gpio` / `i2c` / `spi` など）、`アダプタ … でペリフェラルを作る` → `GATT application registered` → `アドバタイズを開始する` まで通り、**iPhone 13 が実際に接続できた。****BlueZ の D-Bus ポリシーも足していない。****残っているのは systemd 経由だけ** ——手で動かすとログインセッションがあるため、`bike-device.service` から起動したときに結果が変わりうる。以下は当初の文。**`bluetooth` グループのユーザーで `RegisterAdvertisement` と `RegisterApplication` が通る**（BlueZ の D-Bus ポリシーを足さずに済む）。systemd から起動するとログインセッションが無いため、手で動かしたときと結果が違いうる | デバイス全体 | 実機で `systemctl status bike-device` と `journalctl` | 2026-08-31 → 2026-09-20 |
| 44 | **確認できた**（2026-09-20）。**`device-info` は 84 バイトで1回**、**`status` の最長は 181 バイト**。**文書の「実測 207 バイト」は外れていた** ——`cfg` を4キーとも埋めたうえで直前の `config` を4キーとも断らせ、`{"last_error":"config: hold1,hold2,hold3,beat_to"}` が出た状態で 181 バイトである（`apps/device/tools/poke_device.py all`）。**MTU 247 どころか、既定の ATT_MTU でなければ余裕で収まる。****`bluezero` が Read の `offset` を無視して毎回すべてを返す**性質は変わらないので、**項目を足すときはここを測り直す。****残っているのは Android 側だけ** | デバイス / モバイル | **済**（`poke_device.py`。Android は別） | 2026-08-31 → 2026-09-20 |
| 45 | **警告の LED の3段階（0.5Hz の点滅 / 2Hz の点滅 / 点灯）が、日中の屋外の明るさの中で、走行中の周辺視で区別できる。**`kind` を光で分けない代わりに、**`lv` の区別だけは光に載せている**（`docs/notifications/arbitration.md`「警告の光は `lv` だけを表す」）。ここが潰れると、光が伝えるのは「何か光った」だけになる。**走行中に警告が届く経路はこれしかない。** **`link` の LED（点灯 / 0.5Hz / 2Hz）と色で見分けられるかも同時に見る** | 警告の出力 | 実機 + 走行（**5 と一緒に見る**） | 2026-09-14 |
| 46 | **警告の保持時間（`lv` 1/2/3 = 3/4/6 秒）と、スマホ側の再送間隔（2 秒）が実走行で妥当。**「危険が去った」は届かないので**時間でしか消せない**（`docs/notifications/arbitration.md`「出し続ける時間と、消える条件」）。短すぎれば危険が続いているのに消え、長すぎれば通り過ぎた危険が出続ける。**再送間隔を一番短い保持時間以上にすると表示が明滅する**ので、片方だけ動かさないこと | 警告の出力 / モバイル | 実機 + 走行 | 2026-08-31 |
| 47 | **`mv` のしきい値（走行 2.0 m/s 即時 / 停止 0.5 m/s を 3 秒）が、1Hz の測位のゆらぎで往復しない。**往復すると停止中の情報が出たり消えたりし、**それ自体が画面を見させる**（`docs/notifications.md`「`mv` のしきい値（スマホ側）」）。**水平精度からの復帰にも 3 秒の待ちを置いてある**。8・38 と同じゆらぎの問題 | 走行中/停止中の判定 | 走行ログを取り、信号待ちの区間を見る | 2026-08-31 |
| 48 | **半分確認できた**（2026-09-19）。`device.main` を 1Hz で回した状態で、**下段の `DOWN  >` がちらつかずに読めている**（実装は変わった行だけ書く。`apps/device/src/device/hw/lcd.py`。#151）。**残っているのは、上下段が同時に変わり続ける場合と長時間。****下段には毎秒進む生存のコマが入った**ので（`docs/notifications/arbitration.md`「桁 4 のコマ」）、**走行中かどうかに関係なく、起動している間ずっと毎秒1行書き直している** ——止まって見える方が異常なので、**ちらつきと読みやすさはこの状態で見ること** | 警告の出力 | 実機（**走行の回に、画面を見ながら1分**） | 2026-08-31 |
| 50 | **心拍のタイムアウト（3 秒）と `t` の窓（5 秒）が実機で妥当。**`beat` は毎秒1通の約束だが、BLE の接続間隔のゆらぎ・GC・フォアグラウンドサービスの制限で**間隔が伸びる。**短すぎると1通遅れただけで `link` が `down` に落ちて表示が明滅し、長すぎるとスマホが落ちたことに気づくのが遅れる（`apps/device/src/device/config.py`）。**25 と一緒に見る**（あちらは発火するか、ここは秒数が妥当か）。**止まった状態では妥当だった**（2026-09-19。iPhone 13 を繋いだまま **61 分**、ラズパイのログに `link` の変化が1行も出ていない ＝ 心拍が 3 秒を超えて途切れていない）。**残っているのは走行中と Android** ——**上の懸念（フォアグラウンドサービスの制限で間隔が伸びる）は Android 固有**で、iPhone 61 分で埋まったのは iOS 側だけである（→ 22 / 32） | デバイス / モバイル（安全） | 実機 + 走行（**Android でも**。`beat` の到着間隔を journalctl で測る） | 2026-08-31 → 2026-09-19 |
| 53 | **止まっている端末から `self` が毎秒出る。**もとは `watchPositionAsync` の `distanceInterval: 0` に頼っていたが、**iOS では外れた**——机の上で 60 分測って中継の間隔が**平均 5.3 秒・最長 30.0 秒**（近傍の失効は 3 秒）。`pausesUpdatesAutomatically: false` を渡していても、**止まっている端末に iOS は測位を出さない**（`timeInterval` は iOS では読まれない。`EXLocationTaskConsumer.m`）。**送る側で 1Hz を作り直すことにした**（`docs/adr/0011-stationary-fix-hold.md`。#167）ので、**確かめるのは保持を入れたあとの挙動**である。**Android 側は取り直していない** | 全検知 / 車車間の位置共有 | **屋外で、ハンドルに載せた状態**で止まったまま `beat` と POST が 1Hz で続くか（机の上・屋内とは条件が違う）。**iOS と Android の両方で**（→ #167 / 39 / 137） | 2026-08-31 |
| 54 | **`Accuracy.BestForNavigation` の消費電力が走行時間に耐える。**精度を落とすと検知の前提が崩れるため最高精度を取りに行っているが、**電池が先に尽きると全部止まる** | 全検知 | 実機 + 1時間以上の連続稼働 | 2026-08-31 |
| 55 | **警告の再送間隔（2 秒）とデバイス側の保持時間の関係が実機で成立する。**再送が保持より遅いと、**危険が続いているのに表示が一瞬消える**（`docs/notifications/arbitration.md`）。**45・46 と同じ走行で見る** | モバイル / デバイス（表示） | 実機 + Android 端末（`lv 1` の警告を続けて出し、表示が途切れないか） | 2026-08-31 |
| 56 | **POST の往復が 1 秒を超えても測位が詰まらない。**往復中の測位では中継を飛ばし、検知だけを回す実装にしてある（`apps/mobile/src/ride/loop.ts`）。**飛ばした回数が多いと、近傍の更新が実質 0.5Hz 以下になる** | 車車間の位置共有 / 全検知 | 実機 + 会場相当の電波環境（飛ばした回数を数える） | 2026-08-31 |
| 57 | **方角の無い測位が「真北」として配られない。**expo-location の Android 実装は `hasBearing()` が false でも `heading` に `0.0` を返す（`records/LocationResults.kt` で確認済み）ため、**速度が出ているのに方角が無い測位を弾く手段がない。**弾いているのは速度の条件（1.0 m/s 未満）だけで、fused provider の network 由来の測位が混ざると `crs: 0` が確かな値として配られ、見えない曲がり角の対向車（#11）がそれを信じる | 車車間の位置共有 / 対向車の検知（#11） | 実機 + Android 端末（東以外へ走って `crs` が 0 に張り付かないか） | 2026-08-31 |
| 59 | **毎周期の `SELECT`（自セル + 周囲8セル）が 1Hz に間に合う。**起動時に全件をメモリへ読む案を捨てて SQL で引くと決めた（`docs/adr/0009-on-device-storage.md`）ので、**索引付きの検索が測位・POST・検知の合計に対して誤差であること**が前提になっている。**遅ければ走行ループごと詰まる**（`docs/unverified.md` 32 と同じ場所で効く） | モバイル（#27）/ 全検知 | 実機 + Android 端末（**32 と一緒に測る**） | 2026-09-01 |
| 60 | **同梱した `signs.db` が実機で開ける。**`SQLiteProvider` の `assetSource` 経由でアプリの資産からデータベースを開く経路で、**Metro の `assetExts` に `db` を足す必要がある**。**外れると、標識ゼロのアプリが黙って出来上がる**——起動時の件数チェックで止まるが、**止まった理由が分かるのは実機で見たときだけ** | モバイル（#27） | 実機 + Android / iOS 端末 | 2026-09-01 |
| 62 | **一時停止の「規制地点」が停止線か交差点中央部か、データからは区別できない。**説明書 3.5.2 はどちらもありうると書いており、**見分ける項目が無い**。交差点中央だと、警告を出す距離が交差点の大きさぶん手前にずれる。**距離の分布は測った**（岡山県 202607 / 28,649 組。規制地点↔進入方向は中央値 20.8m、p10 12m・p90 32m・99.4% が 50m 以内）が、**この分布ではどちらとも決まらない**——進入方向が道路上のどこに置かれるかも同じだけ未知だから | 一時停止の事前通知（#27） | **数箇所を航空写真と重ねて、規制地点が停止線の上か交差点の中心かを目で見る**（実機不要） | 2026-09-01 |
| 63 | **欠番。**ユニークキーが月をまたいで同じ標識に同じ値を保つか、を確かめる前提だったが、**いまある原本（岡山県警 202607）のままデモを迎えると決めた**（2026-09-19）ので、**月をまたぐ取り込みそのものが起きない。****確かめる必要が消えた**のであって、外れても壊れないと分かったわけではない——**新しい月を取り込むなら、この行を立て直すこと** | — | — | 2026-09-01 → 2026-09-19 |
| 64 | **不停止のしきい値（停止 1.5 m/s / 標識から 20m / 進入方向のずれ 60°）が実走行で妥当**（**測位精度の足切りは 71**）（`docs/interfaces/web-stats.md`「しきい値の既定値」）。**1Hz の測位では1秒より短い停止が点として現れない**（34）ので、**きちんと止まったのに不停止と判定される**ことがありうる。**規制地点が停止線か交差点中央部か決まっていない**（62）ため、20m という距離の当たりも付いていない。**濡れ衣を着せる方が悪いので、外すなら見逃す側へ倒す** | 不停止の判定（Web） | **実走行のログを1回取れば、走らずに何通りでも試せる**（`POST /api/admin/recompute`。生ログを残しているのはこのため。`docs/adr/0007-keep-raw-ride-logs.md`） | 2026-09-01 |
| 65 | **順位に出す通過の下限 5走行で、ランキングに行が残る。**率で順位を付けると決めた（`docs/interfaces/web-stats.md`「率で見る」）ので、**同じセルを複数の走行が通っていないと分母が立たない。**走る人が数人しかいない規模では、**下限を満たすセルが1つも無い＝ランキングが空**という結果がありうる。**そのときに下げると、今度は1回しか通っていない場所で上位が埋まる** | ランキング（Web） | **走行ログが集まった時点で、下限ごとに残るセルの数を数える**（実機不要） | 2026-09-01 |
| 67 | **`app.db` の移行（`PRAGMA user_version` を読んで DDL を流す）が `expo-sqlite` の Drizzle ドライバで動く。**Node の `better-sqlite3` では確かめてある（`apps/mobile/src/log/store.test.ts`）が、**`PRAGMA` を `prepare` 経由で読む経路はドライバごとに違いうる。**外れると**アプリが走行ログの表を作れず、走行のたびに保存が落ちる**（画面には出るが、走行そのものは止まらない） | 走行ログ（#73） | 実機 + Android 端末（1回走って `pending` の件数が増えるか） | 2026-09-01 |
| 68 | **走行中の 1Hz の `INSERT`（測位1点）が、測位のコールバックの中で詰まらない。**`SELECT` の側は 59 で見るが、**書き込みは端末のストレージに触れる**ぶん別物である。外れると**測位・中継・検知が丸ごと遅れる**——溜めるのが目的の処理で、目的の方を殺すことになる | 走行ログ（#73）/ 全検知 | 実機 + Android 端末（**59 と一緒に測る**） | 2026-09-01 |
| 69 | **送り終えた走行ログを端末に置いておく期間（3日）が妥当**（`apps/mobile/src/log/config.ts`）。**サーバー側にある確証は「200 が返ったこと」だけ**（取り込みは受け取った件数を返し、入った件数は返さない）なので、**取りこぼしに気づいてから手元を見に行けるだけの猶予**として置いた。**短すぎると確かめる術がなくなり、長すぎると位置情報を端末に残す期間がそのまま延びる**（走行ログは個人情報である。`AGENTS.md`） | 走行ログ（#90） | **デモまでの運用で決める**（実機不要。取り込みの取りこぼしが実際に起きるかを見てから詰める） | 2026-09-01 |
| 70 | **画面を開いたときの掃除（`purgeRideLogs`）が、最初の描画を詰まらせない。****同期の読み書き**（数え上げ3文 + 削除3文）が**ホーム画面の初回描画で JS スレッドの上を走る**（`apps/mobile/src/log/use-ride-log-sync.ts`）。**しばらく開かなかった端末では、数万行を相手にこれが走る。**外れると、**アプリを開いた直後だけ操作を受け付けない** | 走行ログ（#90） | 実機 + Android 端末（**68 と一緒に測る**。同じ走行で済む） | 2026-09-01 |
| 71 | **判定から外す測位精度（30m）が実走行で妥当**（`docs/interfaces/web-stats.md`「しきい値の既定値」）。**取り込みは精度で足切りをしない**ので（`apps/web/src/worker/logs/config.ts`）、**捨てるのは判定のときだけ**である。**厳しくするとビルの谷間の交差点が丸ごと判定から消え**——そこは一番見たい場所でもある——**緩めると、20m の半径が精度の中に埋もれる。**64 と同じデータで一緒に振れる | 不停止の判定（Web） | **実走行のログを1回取れば、走らずに何通りでも試せる**（`POST /api/admin/recompute`） | 2026-09-02 |
| 73 | **検知を測位点に突き合わせる許容のずれ（5秒）が実走行で妥当**（`apps/web/src/worker/stats/config.ts`）。**測位が 1Hz で出ている前提**の値で、**測位が出ていない（`nofix`）間の検知は「場所不明」に落とす**ためにある。**広げると、何十秒も離れた別の場所の点に検知が付く。狭めると、正しく突き合うはずの検知が場所不明に落ちる**——どちらも地図と順位が静かにずれる | 集計（Web） | **実走行のログを1回取れば、走らずに何通りでも試せる**（`GET /api/stats/cells`。応答の `unlocated` の数を見る） | 2026-09-02 |
| 75 | **一時停止の事前通知のしきい値が実走行で妥当**（**5 秒手前**／距離は 15〜60m に丸める／その**手前半分から `lv` を 2 に上げる**／進入方向のずれ **45°** 以内／前方と見なす角度 **60°**／**1.5 m/s 以下と、標識まで 8m を切ったら黙る**／測位が **3 秒**古かったら黙る。`apps/mobile/src/detect/stop-sign.ts`）。**早すぎれば「いつも鳴っているもの」になり、遅ければ止まれない。****サーバー側の不停止の判定（64）とは別の値**で、あちらは走り終えたログを見る後追いの判定、こちらは走行中の事前通知である | 標識の検知（#27） | 実機 + 走行（**5・9 と一緒に見る**） | 2026-09-02 |
| 76 | **標識が道路の脇に立っていても、進入方向の直線と実際の進行方角が 45° 以内に収まる。****標識の直前で道が曲がっていると、この2つはずれる**（`approach` から規制地点へ向かう直線は、カーブに沿っていない）。外れると**カーブの先の標識だけが黙り、鳴らないことは走行中に気づけない** | 標識の検知（#27） | 実機 + 現地（カーブの手前にある標識で確かめる） | 2026-09-02 |
| 80 | **前方急ブレーキのしきい値が実走行で妥当**（減速度 **2.0 m/s²** 以上／**窓 3 秒**の中で速度が **2.0 m/s** 以上落ちていること／前方と見なす角度 **60°**／同じ向きと見なす角度 **60°**／**60m** 以内で、**8m を切ったら黙る**。`apps/mobile/src/detect/hard-brake.ts`）。**自転車が普通に止まるときの減速（1〜1.5 m/s²）の上に置いた**が、実測ではない。低ければ**信号の手前で普通に止まる前の車両で鳴り続け**、高ければ本当の急ブレーキを見逃す | 前方急ブレーキの検知（#10） | 実機2台 + 走行（**5・36 と一緒に見る**） | 2026-09-02 |
| 81 | **測位の速度（`spd`）のゆらぎが、窓の中の最も強い区間を選ぶ方式に耐える**（`apps/mobile/src/detect/hard-brake.ts` の `hardestBraking`）。**窓の中のすべての組から一番強い減速を選ぶ**ので、**1組でもゆらげばそれが選ばれる。**落ち幅の足切り（2.0 m/s）で弾く前提だが、**1Hz では 1 秒で 2 m/s ゆらげば通ってしまう。**38 と同じゆらぎの問題で、あちらは位置、こちらは速度 | 前方急ブレーキの検知（#10） | 走行ログを取り、等速で走っている区間の `spd` の振れ幅を見る | 2026-09-02 |
| 82 | **「見えない」を「相手が自分の正面から 30° 以上外れている」で近似したことが、実際の見通しと合う**（`apps/mobile/src/detect/blind-corner.ts`）。**建物の位置はデータに無い**ので、幾何以外に判断の材料が無い。**外れ方は両方向にある**——見通しの利く交差点で鳴り続けるか、角の建物で完全に隠れている相手を「正面だから見えている」として落とすか。**後者は静かに黙る** | 見えない曲がり角の検知（#11） | 実機2台 + 走行（**見通しの悪い交差点と、良い交差点の両方で見る**） | 2026-09-02 |
| 83 | **見えない曲がり角のしきい値が実走行で妥当**（交点まで **8 秒**以内／到達時刻の差 **3 秒**以内／進路の角度差 **30〜150°**／正面から **30°** 以上外れていること／両者とも **1.5 m/s** 以上で、**8m を切ったら黙る**。`apps/mobile/src/detect/blind-corner.ts`）。**進路を「いまの方角へまっすぐ」として交点を求めている**ので、**曲がっている最中の相手では交点が実際とずれる。**加えて **57**（方角の無い測位が `crs: 0` として配られる）が当たると、**真北へ進む進路を引いた幻の交点**で鳴る | 見えない曲がり角の検知（#11） | 実機2台 + 走行（**5・57 と一緒に見る**） | 2026-09-02 |
| 84 | **`GET /api/health` に届くことが、中継（`POST /api/v2v/exchange`）が届くことの代理になる。**走行前の点検はこの1回の往復でサーバーを緑にしている（`apps/mobile/src/ride/server-reach.ts`）が、**同じ Worker でも通る先は違う**——Durable Object 側だけが詰まる故障は**緑のまま走り出せる。**走り出せば `postFailures` が拾うが、**それは走行中で、人はもう画面を見られない** | 走行前の点検（#70） | 実機 + 会場相当の電波環境（**23・27 と一緒に見る**） | 2026-09-02 |
| 85 | **「4つ全部が緑でなければ走り出せない」が、会場の運用として成立する。**静かに黙る故障を出発前に止めるための設計だが（`apps/mobile/src/ride/pre-ride.ts`）、**電波の弱い場所では誰も出発できない。**外れると、**デモの最中に人が「押せない」で止まる**——そのとき緩めるのは**サーバーの項目だけ**（デバイス・標識・測位が欠けた走行は、黙ったまま走ることそのものである） | 走行前の点検（#70） | デモ会場で、出発地点に立って点検が緑になるか | 2026-09-02 |
| 86 | **端末が県を選び直したあと、走行までに一度もアプリを落とさなくても検知が新しい県の標識を引く。**入れ替えは同じ接続（`useSQLiteContext()`）を通しており、読む側は用意した文（`prepare()`）を使い回している（`apps/mobile/src/signs/store.ts`）。**用意した文が入れ替え前のテーブルを掴んだままなら、選び直したのに古い県で走る**——鳴らないことは走行中に気づけない | 都道府県の選択（#71） | 実機 + Android 端末（**県を選び直し、アプリを落とさずに走行を始めて、新しい県の標識で鳴るか**） | 2026-09-02 |
| 87 | **他県を D1 に取り込んだとき、`GET /api/stop-signs/prefs` に出た県がそのまま配れる。**一覧は版の表（`stop_sign_versions`）だけを読んでおり、**標識の本体が欠けていても一覧には出る**（`apps/web/src/worker/stop-signs/query.ts`）。取り込みは D1 でトランザクションを張れず、途中で落ちれば版だけが残る——**選べるのに 500 が返る県**になる。いまは岡山県しか無いので起きていない | 都道府県の選択（#71） | 他県を取り込んだとき（**取り込みが途中で落ちたあと、一覧と取得の食い違いを見る**） | 2026-09-02 |
| 88 | **iOS で確認できた**（2026-09-19）。iPhone 13 から実機のラズパイ（BlueZ 5.82）へ、**スキャン → 接続 → MTU 要求 → サービス探索 → `device-info` → `status` の購読**が全部通り、**切断からの戻りも通った**（→ 12 / 96 / 91）。**残っているのは Android**（あちらは macOS の疑似ペリフェラル相手しか見ていない）。**この行を消さない** ——iOS が済んだからといって落とすと、**Android の接続手順が未確認のまま記録から消える** | モバイル / デバイス | 実機 + Android 端末 | 2026-09-03 → 2026-09-19 |
| 89 | **画面を消したまま、心拍が走行中ずっと 1Hz で出続ける。** React Native は画面が消えるとタイマーのフレームコールバックを外すので **`setInterval` が事実上止まる**（`JavaTimerManager.onHostPause()`。実機で確認済み——画面を消した瞬間に毎秒から十数秒に1通へ落ちた）。そこで**測位の到着からも心拍を出す**ようにした（`apps/mobile/src/ride/loop.ts` の `beat()`）が、**測位が途切れている間に画面を消していると、心拍の供給源が両方とも止まる**——デバイスは `link` を `down`（＝アプリが落ちた）にする一方、アプリは動いている | 全検知（安全） | 実機 + 走行（**画面を消したまま、測位の切れる場所を通る**） | 2026-09-03 |
| 90 | **走行中に BLE が切れて再接続することが、実際にどれだけ起きるか。**`config` は**切断で既定へ戻る**設計にした（`docs/interfaces/ble-gatt.md`「`config`」）ので、**再接続のたびにアプリが書き直す前提**で成り立っている。書き直しが漏れると、**同じ走行の途中から警告の秒数が変わる**——変わったこと自体は走行中に気づけない（`status` の `cfg` は走行後にしか読めない）。**再接続がほとんど起きないなら実害も小さい** | モバイル / デバイス（表示） | 実機 + 走行（**88 と一緒に見る**。切断と再接続の回数を数える） | 2026-09-05 |
| 91 | **確認できた**（2026-09-19）。ラズパイのログで **`切断された` → 6 秒後に `接続された`** が3回繰り返された（心拍が途切れた接続を 30 秒で切ったあとの、デバイス発の切断である）。**`register_advertisement` を1回しか呼ばなくても、BlueZ は切断のたびにアドバタイズへ戻る。****`on_disconnect` で登録し直す必要は無い。****次に消すときにこの行ごと落とす**。以下は当初の文。**デバイスが自分から切ったあと、アドバタイズが自動で再開する。**心拍の来ない接続を 30 秒で切る（`docs/interfaces/ble-gatt.md`「前提」）のは、**切れば持ち主が繋ぎ直せる**という前提の上に立っている。bluezero は `register_advertisement` を `publish()` で1回呼ぶだけで、**切断のたびに登録し直さない**（upstream の `peripheral.py` で確認済み）ので、**再開するかは BlueZ の挙動しだい。****外れると、切ったせいで誰も繋げなくなる**——再起動を無くすつもりで、再起動を必須にする。外れていたら `on_disconnect` で登録し直す | デバイス / モバイル | 実機 + Android 端末（nRF Connect で繋ぎっぱなしにし、30 秒後に切れてから**アプリで拾えるか**） | 2026-09-05 |
| 92 | **AE-NJR4265 J1 が、走ってくる自転車・自動車を検知できる。**データシートの検知可能速度範囲は **0.25〜1.0 m/s**（時速 0.9〜3.6km）で、**後方から来る自動車（約 11 m/s）も自転車（約 4 m/s）も範囲の外**にある。加えて「5. 環境雑音低減処理」が**歩行者の連続的な動きを拾い、突発的な動きを落とす**方向に効くため、**速い移動体が雑音として捨てられる**可能性がある。**デモの条件（止まった自転車の後ろから人が歩いて近づく）は範囲の中**なので、**デモは成立するが実走行では出ないかもしれない。**外れたら**センサーの方式ごと差し替える**ことになる（`docs/hardware.md`） | ローカル検知（#26） | 実機 + 部品（**まず人が歩いて近づく／次に自転車で追い越してもらう**の2段で見る。**26 と一緒に**） | 2026-09-08 |
| 94 | **ラズパイの 3.3V ピンから 60mA（AE-NJR4265）を取っても、電圧が落ちない。****LCD は 5V へ移したので、この行の前提から外れた**（2026-09-19。`docs/hardware.md`「表示」）。LCD だけを載せた状態では `vcgencmd get_throttled` が `0x0` だった。`adr/0003` のとおり 3.3V レールで使える電流は 5V より小さい。**外れると、走行中に再起動する**——落ちる理由が電源だと気づくまでに時間がかかる種類の故障である。外れたら**検知出力を分圧して 5V へ移す** | デバイス全体 | 実機（**AE-NJR4265 を挿した状態で `vcgencmd get_throttled` を見る**——`0x0` 以外なら電圧が落ちている。**電源 LED は無い**（緑の ACT が1つだけ）ので、目で見て分かるのは再起動したときだけである） | 2026-09-08 |
| 95 | **iOS で、画面を消したまま測位 1Hz と BLE の心拍が走行時間ぶん続く。****常駐は取れた**——iPhone 13（iOS 26.6.2、Release ビルド）で **60 分間、心拍の途切れ 0 回・切断 0 回**、中継 681 通すべて成功（2026-09-14。相手は `apps/device/tools/mock_peripheral.py`）。**残っているのは測位の間隔。**机の上で**止めて**測ったため、中継の間隔は平均 5.3 秒・**最長 30.0 秒**になった。**心拍は途切れていないのでアプリは生きており、測位だけが止まっている**（動かない端末に iOS が更新を出さない）。**動いていれば 1Hz が出るのかは、動かさないと分からない**——外れると時速 20km で約 170m を見落とす。**止まっている側は送る側で埋めることにした**（`docs/adr/0011-stationary-fix-hold.md`。53 / 101 / 102）ので、**この行に残るのは「走っている間」だけ**である | 全検知 | **実走行**で測位の間隔を見る（→ 139 / 140）。**常駐そのものは確認済み**なので、残りは「動かすと 1Hz が出るか」だけ | 2026-09-14 |
| 96 | **ほぼ確認できた**（2026-09-19）。**実値だけが残っている**（下の末尾）。**iOS がラズパイとの間で MTU 163 以上を取る。****アプリ側は動いた**——iPhone 13（iOS 26.6.2）から macOS の疑似ペリフェラル相手に **515** が返った。`requestMTU` は**要求値を捨ててその場の値を即返すだけ**で（`MultiplatformBleAdapter` の `iOS/classes/BleModule.swift`。ソースで確認済み）、**その戻り値から読むと必ず既定の 23 になり、必ず弾かれていた**（2026-09-14 に修正）。下回れば `apps/mobile/src/ble/link.ts` が接続を諦めるので**静かには壊れない**が、**諦めた先に打つ手が無い**。**ラズパイ相手でも 163 以上は取れている**（2026-09-19）——**163 未満なら `apps/mobile/src/ble/link.ts` が接続を諦める**作りで、iPhone 13 が実機につながって `device-info` まで読めたため。**正確な値だけが残っている**（release ビルドでは `console.log` が端末のコンソールに流れないので、Metro を繋いだ回に見る） | デバイス / モバイル | **ほぼ済**（値だけ `[ble] MTU=` のログで確かめる。**13 の iOS 版**） | 2026-09-14 → 2026-09-19 |
| 98 | **iOS が GATT テーブルをキャッシュしたまま、古い構成を掴み続けない。**iOS はペリフェラルのサービス構成を端末側にキャッシュし、**Service Changed の指示が無いと再探索しない。**デバイス側に characteristic を足した直後、**iPhone だけが前の構成のまま**になりうる。**外れると「デバイスは新しいのにアプリからは見えない」**という、どちらを疑えばいいか分からない形で出る（Bluetooth の設定から一度忘れさせると直る） | デバイス / モバイル | 実機 + iPhone（`docs/interfaces/ble-gatt.md` に項目を足した回に、**アプリを入れ直さずに**読めるか） | 2026-09-14 |
| 99 | **半分外れた**（2026-09-19）。**出力は通る** ——`NativeFactory` に落ちて `LED(22).on()` で GPIO22 が `1` になった。**入力は落ちる** ——`DigitalInputDevice` は `when_activated` のために**エッジ検出を有効にし、その実装が sysfs の `/sys/class/gpio/export` を叩く**が、**いまのカーネル（Debian 13）に sysfs GPIO は無い**（`OSError: [Errno 22] Invalid argument` で `device.main` ごと止まった）。**毎秒読むだけならエッジは要らない**ので `InputDevice` に替えて回避した（`apps/device/src/device/hw/rear_sensor.py`）。**エッジ検出が要る部品を足すときは、ここで `rpi-lgpio` を入れることになる** | 警告の出力（LED ×2）/ ローカル検知 | **済**（回避の上で） | 2026-09-14 → 2026-09-19 |
| 101 | **屋内の机の上で測った結果が、屋外でも同じ。**実測（#166。→ 53）は**屋内の机の上**で取っており、GNSS ではなく Wi-Fi 測位に落ちていた可能性がある。**屋外・ハンドル固定で止まったときに 1Hz が出るなら、保持（`docs/adr/0011-stationary-fix-hold.md`）は動く場面が無い**——入れておいて害は無いが、**この行が残っている間は「iOS では止まると測位が来ない」を確定した事実として扱わない** | 車車間の位置共有 / 全検知 | **屋外**で、止まった状態と走っている状態の両方で更新の間隔を測る（→ #167 / 39） | 2026-09-14 |
| 102 | **`activityType`（`Fitness`）を渡すと、止まっていても iOS が測位を出す。**いまは渡しておらず、既定は `CLActivityTypeOther`。**効けば保持（`docs/adr/0011-stationary-fix-hold.md`）は動く場面が無くなる。****見込みは薄い**——`pausesUpdatesAutomatically: false` は既に渡しており、Apple のドキュメント上`activityType` は主に休止の判断に使われる。**1行なので入れるが、結果を待たない** | 車車間の位置共有 / 全検知 | **101 と同じ回で分かる**（保持した回数が 0 なら効いている） | 2026-09-14 |
| 103 | **後方物体検知のソフトウェア側の値（H が 1 秒続いたら発火 / 次まで 3 秒空ける）が妥当**（`apps/device/src/device/config.py` の `REAR_CONFIG`）。**読むのは毎秒1回**なので、**2 周期続けて H だったとき**に出る（検知から最大 2 秒）。短くすると**ドップラーが拾った一瞬のノイズがそのまま警告になり**、長くすると**近づいてくるものに対して出るのが遅れる。****クールダウンは `hold_ms` の `lv 2`（4 秒）より短くしてある**——長いと、後ろに物体が居続けている間に**表示と光が 1 周期まるごと消える。****検知距離はここに無い**（VR1 のツマミが正本。`docs/hardware.md`） | ローカル検知（#26） | 実機 + 部品（**92 と同じ回に、人が歩いて近づいて見る**） | 2026-09-19 |
| 105 | **Zero 2 W の消費電流の見積もり（250mA）が妥当。****当初は Zero W の 150mA で電源を見積もっていた**（`docs/hardware.md` の表）が、**実機は Zero 2 W（4コア）で、より食う。**外れると**走行中に再起動する**——7（電源が走行中もつ）と 94（3.3V レールが落ちない）の両方の前提になっている数字である | デバイス全体 | 実機 + 電流計（**7 と一緒に見る**） | 2026-09-19 |
| 106 | **確認できた**（2026-09-19）。**一度きりで済む。**iPhone 17（iOS 26.6.1）でも同じで、**アプリを開き直した直後の1回だけ心拍が 4 秒で止まり**、デバイスが 30 秒で切って、**繋ぎ直したあとは 2 分 38 秒まったく途切れなかった**（09:28:19 → 09:28:23 で `down`、09:28:50 切断、09:28:57 再接続、09:28:59 `nofix` 以降そのまま）。**端末固有ではない。**実害は**走り出す直前に開き直すと 30 秒待たされる**ことだけ。**次に消すときにこの行ごと落とす** | モバイル / デバイス | **済** | 2026-09-19 |
| 107 | **まだ見られない**（2026-09-20）。**手元の検知ログが1件しか無く、塊が1つで終わる**ため、飛びようがない（`poke_device.py all` で `塊 1 個 / 82 バイト`）。**数百件を積むまで確かめられない。**以下は当初の文。**`set_value()` を 20ms ごとに呼んで、検知ログの Notify が取りこぼされずに届く**（#40。`apps/device/src/device/config.py` の `LOG_CHUNK_INTERVAL_MS`）。bluezero の `set_value()` は D-Bus の `PropertiesChanged` を出すだけなので、**BlueZ が送り出す前に次を重ねると、間の塊が消えうる**。消えると JSON の行が欠け、**そのレコードだけが黙って落ちる**（`seq` の飛びとして見えるので、セントラルは1回取り直す）。**詰めるほど速く、空けるほど安全**で、**実機で測るまでどちらとも言えない** | デバイス / モバイル（#40） | 実機 + iPhone（数百件を流して、届いた `seq` に飛びが無いか） | 2026-09-19 |
| 108 | **確認できた**（2026-09-20）。**BlueZ は `WriteValue` の `options` に `mtu` を入れて渡す** ——`検知ログの転送を始める（since=0 / 1 件 / 82 バイト / **1回 514 バイト**）` が出た。**514 = MTU 517 − 3** で、`fallback_chunk` には倒れていない（相手は macOS の CoreBluetooth。`apps/device/tools/poke_device.py`）。**実機の BlueZ は 5.82**（→ 15）。**iPhone 相手の値は別**で、あちらは 96 で見る。**次に消すときにこの行ごと落とす** | デバイス / モバイル（#40） | **済** | 2026-09-19 → 2026-09-20 |
| 109 | **確認できた**（2026-09-19）。**接続中もアドバタイズは出ている**（繋がっている実機に2台目が後から繋がった）。**2台目は切られ、主の接続は残った** ——`2台目を切る（主は 4A:98:…）` の直後に `主でない相手が切れた（何もしない）` が出て、**主の転送も設定も巻き添えになっていない**（#184 / PR #187）。**次に消すときにこの行ごと落とす** | デバイス / モバイル | **済** | 2026-09-19 |
| 110 | **`disconnect_central()` が失敗したときに主を手放す直し方で、締め出しが起きなくなる。****2026-09-19 に実機で締め出しが起きた** ——心拍が途切れた接続を切ろうとしたら `ServiceUnknown: The name :1.2 was not provided by any .service files` で失敗し、**以後 30 秒ごとに失敗し続けた**（10 回）。**BlueZ がデバイスのオブジェクトごと消すと `InterfacesRemoved` になるが、bluezero は `PropertiesChanged` の `Connected` からしか `on_disconnect` を呼ばない**ので、**切断が誰にも知らされず主が残る。**残ると `SingleCentral` が**新しい相手をすべて2台目として断る** ——**切ったつもりで締め出していた**（電源を入れ直すまで戻らない）。**失敗したら主を手放す**ようにしたが、**同じ経路をもう一度踏ませて確かめていない** | デバイス / モバイル | 実機（心拍を書かないまま 30 秒放置 → **そのあと繋ぎ直せるか**）。**相手が本当に残っていた場合は2台つながる**が、**1台も繋がらないより軽い**と判断した | 2026-09-19 |
| 112 | **`plugins/with-scene-delegate.js` が、本物の `expo prebuild` の中でも当たる。****当て先は upstream のテンプレート実物（`expo-template-bare-minimum@57.0.26`）で確かめてある**——当たること・旧ウィンドウ生成が消えること・二度当てても増えないことまで見た（2026-09-20）。**確かめていないのは `prebuild` を通した経路そのもの**で、いまの `ios/` は 2026-09-14 に生成したものを使い続けている。**外れても静かには壊れない** ——当て先を見失ったらプラグインが例外で止まる作りにしてある（`replaceOnce()`）。**expo の SDK を上げた回に、まずここを見る** | モバイル（iOS のビルド） | `cd apps/mobile && npx expo prebuild --platform ios --clean` を通してから Release ビルド。**`prebuild` は `ios/` を作り直すので Team を選び直すこと**（`docs/deploy-mobile.md` の 2-2） | 2026-09-20 |

**20 番と 24 番は欠番。** 20 は「2.4GHz のブロードキャストの取りこぼし」、24 は「`uplink` が毎秒何通通るか」で、
どちらも**経路が変わって前提ごと消えた**。**書き換えずに欠番のままにする**
（番号を使い回すと、その変更より前の「20番」「24番」への言及が別の前提を指す）。
20 は 27 として立て直した。**24 に対応する新しい前提は無い**——測る対象そのものが無くなったため。

## 優先順位

**62 は実機を必要としない。** 原本と地図があれば机上で確かめられる。
**62 は一時停止の事前通知が鳴る距離に直に効く。** #27 は入ったので（PR #95）、
**確かめた結果は 75 のしきい値（何秒手前で知らせるか）の調整として入る。**
34 と 35 は走行ログが取れてからで、**実機の順番待ちには入らない。**

**28 は順番の外にある。** 他と違い、確認しないまま配線すると**基板が物理的に壊れる**。
新しい部品の信号線を初めて挿す前に必ず測る（`docs/adr/0003-hardware-wiring.md`）。
**残っているのは AE-NJR4265 の検知出力**（VDD がそのまま出るので 3.3V で駆動していることを確かめる）。
**LCD の SDA / SCL は片付いた**——5V 駆動に移したうえで、引き下げ抵抗で 3.34V に収めてある
（`docs/hardware.md`「表示」）。**列に並ばせず、挿す直前に測る。**

**ただしテスターが手元に無い**（2026-09-19）。**28 は道具が要る唯一の行**なので、
**AE-NJR4265 を挿す回までに1本用意する。**無いまま挿すと、この行は「確認しないまま配線した」になる。

**40 → 41 → 42 は、ラズパイに触れたとき最初に見る。** この3つは
**外れているとデバイス側が `uv sync` か import の段階で止まり、以降の確認が1つも進められない**
（#52 / `adr/0008-device-dependencies.md`）。手順は `deploy-device.md` を上からなぞるだけで、
`python3 -V` → `uv sync --group device` → `import bluezero` の3手で済む。
15（BlueZ の版）も同じ場で確かめられるので、電源を入れたついでに一緒に見る。
**43 と 44 と 12 は、デバイスを起動してスマホで1回スキャンすれば3つとも分かる**（広告が出るか / 名前が7文字あるか / `device-info` が読めるか）。
**49 も同じ場で見られる**——nRF Connect で `alert` に1通書き、`status` の `warns` が増えるかを見るだけでよい。

実機に触れる機会が来たら **40 → 41 → 42 → 22 → 21 → 30 → 32 → 15 → 43 → 12 → 44 → 49 → 4 → 25 → 94 → 92 → 26 → 48 → 31 → 27 → 29 → 23 → 3 → 14 → 13 → 11 → 8 → 7 → 9 → 5 → 45 → 46 → 47** の順で潰す。

**22 と 21 と 30 を先頭に置くのは、この3つだけがラズパイを必要としないため。**
スマホと簡単な確認用アプリがあれば今日から確かめられる。実機の到着を待つ理由が無い。
**30 と 32 は 22 と同じ「1時間走らせる」確認なので、一度に済ませられる。**
**32 が先頭付近に来たのは、判断がスマホへ移ったため**（`docs/adr/0006-decision-layer-on-mobile.md`）。
スマホが処理しきれなければ、**検知が丸ごと遅れる。**

**その中でも 22 が最優先。** アプリが常駐しなければ**測位も車車間通信も届かず、検知が丸ごと動かない。**
しかも規格どおりに実装しても端末次第で落ちるため、**書き方では解決できず、
どの端末を使うかという選択の問題になる。**遅く気づくほど選び直せない。

21 を早めに置くのは、**精度が足りなければ GPS モジュールを戻す判断になり、
配線と部品の調達が発生する**ため。判断が遅れるほど間に合わなくなる。

**15 → 4 が、ここから先すべての前提。** 15 は**コマンド1つで分かり、駄目なら OS を入れ直す**
ことになるため先に済ませる。4（BLE が通る）が確認できるまで、**25 以降はどれも確かめられない。**

25（ウォッチドッグ）を 4 の直後に置く。22 が外れる（落ちる端末がある）ことは十分あり得るので、
**落ちたときに人が気づける**ことが早く要る。**ここが動かないなら、
スマホ経由という決定そのものを採ってはいけない**（`adr/0004-v2v-transport.md`）。
**判断までスマホへ移した以上（`adr/0006-decision-layer-on-mobile.md`）、
スマホが黙ったときにデバイスが出せるものは心拍の途切れだけ**になった。
**25 は「あると良い確認」ではなく、0006 が成立する条件そのものである。**

**26（後方物体検知）を早めに引き上げた。** デバイスに残る唯一の検知になり、
**スマホが落ちたときに生き残るものがこれしか無い**ため。

29 は 27 と一緒に見る。**どちらも実機2台をつないだ状態でしか分からず、外すと「つながっているのに
相手が見えない」という同じ症状になる**ため、切り分けを一度に済ませる。
**しきい値の既定値は、この確認をするまで暫定**として扱う。

**31 → 27 → 29 → 23 は続けて並べてある。** 下に書くとおり1組で見るものなので、
間に別のものを挟むと、**同じ電波環境へ2度行くことになる**（3 を後ろへ動かしたのはこのため）。

31 と 27 と 23 は**経路が実用になるかを1組で見る**。31（多数の `peer` を1秒で処理しきるか）と
27（失効の秒数）は、どちらも人が集まった状態でしか分からない。
23 は会場に行かないと分からないので、**同じ電波環境で 27 と一緒に測る。**
**以前ここには 24（`uplink` が毎秒何通通るか）が入っていたが、欠番になった**
（`docs/adr/0006-decision-layer-on-mobile.md` で位置を BLE で運ばなくなったため）。

14・13・11・12 は**走行ログの転送に効くもの**で、検知が動くこと（上）より後でよい。
この4つの中では、**仕様をやり直すことになるものを先に置く**。
14 が崩れる（取りこぼす）と、外したアプリ層の再送を戻すことになって転送の設計をやり直す。
13 → 11 → 12 は、そのあとに調整で済む順に並べている。

**26 と 92 は部品が決まったので前に出した**（#13。AE-NJR4265 J1）。
**92 が 26 の前提**である——**速度範囲を外していれば、屋外で実用かどうか以前に何も検知しない。**
どちらも実機に部品を挿せば確かめられ、**走行の順番待ちに入らない**（最初の一歩は
「人が歩いて近づく」だけで済む）。

**37 と 39 は 5 と一緒に見る。** どれも「しきい値が実走行で妥当か」を問うもので、
**シミュレータで発火したことは、実機で発火することの証明にならない**（ゆらぎが無いため）。
逆にシミュレータで出ない誤検知は、実走行でしか見つからない。

**45・46・47 は 5 の直後に置く。** どれも「実走行でしきい値が妥当か」を問うもので、
**同じ走行で一度に測れる**（音が聞こえたか・警告が何秒出ていたか・信号待ちで表示が往復したか）。
**上に書いた 37・39 も同じ走行に含める。** 実機2台を出す機会は多くない。

**50 は 25 と一緒に見る。** どちらも心拍のウォッチドッグで、
**25 が「落ちたことに気づけるか」、50 が「気づくまでの秒数が妥当か」**である。
アプリを落として `down` を確認する走行で、`beat` の到着間隔もあわせて測れる。

**99 は部品を挿すだけで分かる。** **外れても依存が1行増えるだけ**で、`hw/` のコードは変わらない（#151）。

**93（3.3V で読めるか）と 100（PCF8574 のピン割り当て）は 2026-09-19 に片付いた。**
93 は外れ——**3.3V では読めず、5V へ移して引き下げ抵抗を足した**（`docs/hardware.md`「表示」）。
100 は当たり——**文字がそのまま出たので、RPLCD の想定と一致している。**
48 が外れていると「毎秒すべて書き直す」という前提が崩れ、
**変わった桁だけ書く実装に変える**ことになる（規則は変わらないが、書き方が変わる）。

**86 は他県を取り込むまで確かめようがない。** 87 と同じで、
**選べる県が岡山県だけの間は「選び直す」ことができない**——同じ県を選び直しても
`If-None-Match` に手元の版が載るので `304` が返り、入れ替えが起きない
（`apps/mobile/src/signs/update.ts`）。**2県目が入った時点で前に出す。**

**87 は他県を取り込むまで確かめようがない。** 26・48 と同じ扱いで、
**取り込む県が増えた時点で前に出す。**

**確認する作業は Issue を立てて行う。** この表は「何を確かめていないか」の記録であり、作業の管理はしない。
