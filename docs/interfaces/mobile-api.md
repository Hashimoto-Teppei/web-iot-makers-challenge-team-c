# モバイル ⇄ API

`../interfaces.md` から分割した。**境界の一覧と、他の境界へのリンクは
[`../interfaces.md`](../interfaces.md) にある。**

この境界は**走行後の同期だけでなく、走行中の中継も担う。**
経路を選んだ理由と却下した案は `../adr/0005-realtime-transport.md`。

```
[スマホ] --1Hz POST /v2v/exchange--> [Worker/Hono] --RPC--> [DO 1個]
[スマホ] <-- 200 { peers: [...] } ---------------------------┘
```

**エンドポイントの型はコードが正本。** `apps/web/src/worker/` のルート定義から
Hono RPC で型が伝播するため、**ここにリクエスト/レスポンスの構造を書き写さない**
（必ず実装とずれる）。このファイルには**型から読み取れないことだけ**を書く。

**流れるペイロードの中身は [`v2v.md`](./v2v.md) が正本。** `self` / `peer` / `nofix` の形、
単位、丸めの桁、受信側の約束はすべてそちら。**この経路のために別の形を作らない。**

Web の画面は API と同じ Worker 内にあるため単一オリジンで、CORS の考慮は不要。
モバイルアプリはネイティブなので CORS の対象外。

### 運び方

**WebSocket でも SSE でもなく、1Hz の HTTP POST 1本。** 自分の位置を送り、
**同じレスポンスで周辺車両を受け取る。**

- **測位が更新されたときだけ送る。** 時間で回さず測位に合わせる（`v2v.md`「送る間隔」と同じ理由）
- **測位が取れていない間は送らない。** 自分の位置が無ければ半径で問い合わせようがない。
  このときスマホは BLE に `nofix` を書き続ける（`v2v.md`「通信断を必ず見せる」）
- **離脱の通知を送らない。** サーバー側は失効（下）で勝手に消える。
  **アプリが落ちたときに送れない通知は、仕組みとして当てにできない**
- **再送しない。** 失敗した POST を送り直さない。位置情報は古ければ捨てたいデータであり、
  次の測位が1秒後に来る。`v2v.md`「受信側の約束」6 と同じ理由

### Worker と Durable Object の約束

**生きている状態は Durable Object 1個のメモリに置く。永続化しない。**

- **DO は1個。** `idFromName()` に固定の名前を渡す。**geohash などでセルに割らない**
  （境界をまたいだ自転車が互いに見えなくなる）
- **`locationHint` を明示的に渡す**（`apac-ne`）。**渡さないと、最初にリクエストした場所に
  置かれる。** US の CI からスモークテストを1回叩くと北米に固定される。
  ヒントは**作成時の1回しか効かず** best effort なので、**名前に世代を付ける**
  （`-v1`）。置き場所を間違えたら名前を変えて作り直す
- **状態はメモリ上の `Map<id, 最新の位置>` だけ。** `ctx.storage` を使わない。
  **飛んでも1秒で直る**（全員が再 POST する）ので、永続化する理由がない
- **DO は RPC メソッドとして呼ぶ。`fetch()` ハンドラにしない。**
  `fetch()` にすると引数を取り出すのに `await request.json()` が要り、
  **下の「`await` を入れない」が成立しなくなる。** RPC なら引数が復元済みで渡る
- **1回の交換は完全に同期処理で書く。`await` を入れない。**
  DO は単一スレッドだが `await` のたびにリクエストが割り込めるため、
  **`await` が無ければ競合が構造的に起きない。**
  ストレージや D1 を触ると必ず `await` が入る
- **`id` は BLE の `device_id` と同じ形**（16進の小文字8文字）。同じデバイスを2つの名前で呼ばない。
  **スマホは、接続中のデバイスから `device-info` で読んだ `device_id` を自分の `id` として名乗る**
  （`ble-gatt.md`）。スマホ側で別の ID を作らない
- **デバイスにつながっていない間は交換しない。** 名乗る `id` が無いだけでなく、
  **受け取った `peer` を渡す先が無い。** ここで交換すると、
  **デバイスの無い「自転車」が他の人の近傍に現れる**

**1回の交換ですること（この順で）:**

1. **失効した相手を捨てる**（**DO が受け取った時刻**が失効の設定より古い）
2. **自分を更新し、受け取った時刻を DO 側で打つ**
3. **半径内かつ自分以外を返す**

**失効の判定に `t` を使わない。** `t` はスマホの時計で打たれた値である。
`v2v.md`「受信側の約束」5 が**デバイス側で `t` を「いま」として使うことを禁じている**のと
同じ理由がサーバー側にも当てはまる。**時計が失効ぶん以上ずれている端末は、
入れた瞬間に失効して「全員から永久に見えない」状態になる**——
しかも本人の画面では POST が成功し続けるので気づけない。
**`0004` と `0005` が最も危険とした静かな部分故障そのもの**である。
**DO は自分の時計で到着時刻を打ち、それで失効を測る。`t` は中身として通すだけ。**

| 設定 | 既定 | 理由 |
| --- | --- | --- |
| 半径 | **300m** | 検知に必要なのは 50m 程度（`../adr/0004-v2v-transport.md`）。接近の履歴を作る余裕を足す。**広めでよい**——実際の絞り込みはスマホ側の窓が担うので、広げても増えるのは JSON のバイト数だけ |
| 失効 | **3秒**（**到着時刻**で測る） | 送信をやめた相手を配るのをやめるため。**デバイス側の失効（`v2v.md` 7）とは足し算になる**（下） |

**サーバー側とデバイス側の失効は、同じ 3 秒だが同じものを測っていない。足し算になる。**
DO は「最後に届いてから3秒」で配るのをやめ、デバイスは「最後に受信してから3秒」で近傍から消す。
**相手が送信をやめる直前の位置を受け取った人は、そこからさらに3秒持ち続ける**ので、
近傍に残る時間は**最悪でおよそ6秒**になる。6 m/s なら**約 36m ぶんの幻**である。
これは `v2v.md` 7 が防ごうとした「通り過ぎたあとも警告が鳴る」そのものなので、
**2つの値を別々に決めず、合計で決めること。**
**既定の 3 + 3 は仮の値**で、`../unverified.md` 29 で測って決め直す。

- **API のレスポンスに台数の上限を置かない。** 20台ぶんでも 2KB 程度で、HTTPS には負担にならない。
  **上限が要るのは BLE の区間だけ**（下）
- **自分を返さない。** デバイス側にも同じ防御があるが（`v2v.md`「受信側の約束」1）、
  **送らない側で止めるのが先**
- **走行の記録（D1）をこのリクエストの中で同期に書かない。** 蓄積が要るなら
  DO のアラームでまとめて流すか `ctx.waitUntil()` で投げる。
  **リアルタイム経路の遅延に蓄積の書き込みを載せない**

### スマホの約束（BLE に渡すまで）

**レスポンスをそのまま BLE に流さない。** BLE は運べる量に天井がある。

`uplink` は Write Request（応答あり）であり、**ATT のトランザクションは同時に1つしか
未応答にできない**（`ble-gatt.md`）。したがって**天井は「接続間隔ごとに1通」**で、
バイト数ではなく**回数**が効く。Android の既定（`CONNECTION_PRIORITY_BALANCED`）は
接続間隔 30〜50ms 程度とされ、**毎秒 20〜33通**にあたる。

**この ms 値は Android の公式リファレンスには載っていない**（AOSP の実装由来で、
端末とチップに依存する）。**探しても見つからないので探さないこと。**
数字を当てにせず、実測（`../unverified.md` 24）で置き換える前提で書く。

**超えたときの壊れ方が最悪である。** キューが伸びると `self` が古い `peer` の後ろで待たされる。
**BLE は測位の唯一の供給元**なので、これは `v2v.md` のウォッチドッグが `link` を落とし、
**検知が丸ごと止まる**ことを意味する。**自転車が多いときほど確実に起きる。**

#### 周期ごとの窓

**周期の始まりは「測位が更新されたとき」。ただし測位が無い間は1秒のタイマーで回す。**
**タイマーを忘れると `nofix` が1通も飛ばない**——測位が無いときは更新イベントが起きないため。
黙るとデバイスは `down`（＝アプリが落ちた）と表示し、
**`v2v.md`「通信断を必ず見せる」が分けようとした「待てば直る」と「直らない」が区別できなくなる。**

1. **送信キューを空にする。** 前の周期で送りきれなかった `peer` は**捨てる**
   （古い位置は無価値。`v2v.md`「送っていない間を埋めない」と同じ）
2. **`self`（測位が無ければ `nofix`）をキューに入れ、すぐ書き始める。**
   **同時に POST を投げ、レスポンスを待たない**
3. **レスポンスが返ってきたら**（150〜350ms 後）、半径内の相手を
   **近い順に、設定の上限（既定 12台）までキューの後ろに足す**
4. 1通ずつ Write Request を送り、応答を待って次へ。**次の周期が始まったら 1 に戻る**

**2 と 3 を1つの手順として書かないこと。** `peer` は自分の POST のレスポンスとして届くので、
**測位の瞬間にはまだ手元に無い。** レスポンスを待ってから `self` を書く実装にすると、
**測位が往復ぶんまるごと遅れ、この節が守ろうとしている「`self` が絶対に遅れない」が崩れる。**

**時間の予算は思ったより狭い。** 悲観側（接続間隔 50ms）では1通 50ms なので、
`self` 1 + `peer` 12 = 13通で **650ms**。`peer` を書き始められるのは早くて 150〜350ms 後なので、
**1秒の周期にぎりぎり収まる程度**しかない。**上限を上げるときはここを一緒に見ること。**

**上限を「台数」ではなく周期の窓として持つのは、壊れるものの単位に合わせるため。**
`../unverified.md` 8 が言うとおり**測位の頻度は後から上げうる**。
「8台」という持ち方だと 2Hz にした瞬間に BLE の負荷が黙って2倍になるが、
窓なら窓が半分の頻度で来るだけで `peer` が自動的に減る。

この形には、**上限の数字を当てにいかなくてよくなる**という効き目がある。

- **`self` が絶対に遅れない。** 毎周期の先頭に固定される
- **実際に流れる台数をリンクの速さが決める。** 速ければ上限まで、遅ければ通った分だけ
- **キューが伸びない。** 毎秒リセットされるので遅延が累積しない
- **上限に張り付いているかどうかが、そのまま「上げてよいか」の答えになる**

**捨てた数を数えて画面に出せるようにしておく**（`v2v.md` と同じ要求）。
**「上限で入らなかった数」と「入れたが送れなかった数」を分けて数えること。**
前者は上限の問題、後者はリンクの問題で、**打つ手が違う。**
**送れなかった数が常に 0 なら、リンクに余裕があるという証拠**になり、
実走行から上限を上げる根拠が勝手に取れる。

**台数を増やしたいときのレバーは接続間隔。**
`requestConnectionPriority(CONNECTION_PRIORITY_HIGH)` で 11.25〜15ms 程度になり**約3倍**。
アプリ側は1行だが、電池を使い、**端末と BlueZ が接続パラメータ更新を受けるかは端末依存**。

**1回の Write に複数台をまとめない**（`v2v.md`。超えた瞬間に全損になる）。
そもそも天井は回数なので、まとめても効果が薄い。

### 失敗したときの約束

**POST が失敗しても、スマホは黙らない。**

| 起きたこと | スマホがすること | デバイスの `link` |
| --- | --- | --- |
| POST が失敗した／返らない | **`self` は BLE に書き続ける**（自分の測位は自分で取れている）。`peer` は空になる | `up` のまま、近傍だけが空 |
| 測位が取れない | POST しない。**`nofix` を BLE に書く** | `nofix` |
| アプリが落ちた／BLE が切れた | 何も書けない | `down` |

**1行目が重要。** サーバーに届かなくても**自車の測位は生きている**ので、
`self` を止めてはいけない。止めると `link` が `down` になり、
**「スマホは動いているのに落ちたことになる」**という誤った表示になる。

**ただしこの状態は人に見えなければならない。** 近傍が空になるのは
「周りに誰もいない」と区別がつかず、**`0004` が一番恐れた「静かに止まる」そのもの。**
スマホ側は**連続して失敗している旨を自分の画面に出す**（走行前後に見る画面。
`CLAUDE.md` のとおり走行中に見る前提の UX にしない）。

**再接続やバックオフの作り込みは要らない。** 次の測位で次の POST が飛ぶ。
**1Hz のポーリングそのものが死活監視になっている**——これがこの方式を選んだ理由の1つ
（`../adr/0005-realtime-transport.md`）。

### なりすましとプライバシー

`v2v.md`「端末ID とプライバシー」と同じ立場を取る。**ここには差分だけを書く。**

- **スマホと Worker の間は HTTPS で守られる。** BLE の区間と違い、経路上では読めない
- **なりすましを防ぐ仕組みは持たない。** Worker に偽の位置を送る端末を止められない。
  ハッカソンの期間内では、署名や端末認証を入れるより動くものを優先する。
  **やらないと決めただけで、要らないわけではない**
- **DO のメモリにしか置かないので、走行が終われば数秒で消える。**
  D1 に書くのは走行の記録だけで、**その設計は別に決める**（未確定）
- **実走行のログをコミットしない。** テストとシミュレータには合成したデータを使う（`CLAUDE.md`）

### 走行後の同期

**走行ログの吸い上げは `log`（BLE）→ スマホ → この API という順で流れる。**
BLE 側の仕様は [`ble-gatt.md`](./ble-gatt.md)。

**取り込みの一意キーは `(device_id, log_id, seq)`**（`ble-gatt.md`）。
**同じレコードが2回届いても1件として扱えること。**
BLE の転送は途中で切れて取り直すことがあり、**重複は例外ではなく通常運転**である。

蓄積したものをどう見せるか（画面・集計）は**未確定**。
