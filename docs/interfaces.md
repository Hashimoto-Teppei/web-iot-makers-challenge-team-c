# コンポーネント間インターフェース

コンポーネントごとに担当が分かれるため、**境界の仕様を先に決める**。ここが暗黙のまま並行実装が進むと破綻する。
変更したら影響を受ける担当に伝わる形（PR の説明）で行うこと。

**このファイルには決まったことだけを書く。** 何を決める必要があるかは Issue で管理する
（`type:design` ラベル）。決まっていない論点をここにチェックリストで置くと、Issue と二重管理になり必ずずれる。

| 境界 | 状態 | 決めるための Issue |
| --- | --- | --- |
| [デバイス ⇄ モバイル（BLE GATT）](#デバイス--モバイルble-gatt) | 未着手 | [#5](https://github.com/Hashimoto-Teppei/web-iot-makers-challenge-team-c/issues/5) |
| [デバイス ⇄ デバイス（メッシュ）](#デバイス--デバイスメッシュ) | 未着手 | [#6](https://github.com/Hashimoto-Teppei/web-iot-makers-challenge-team-c/issues/6) |
| [モバイル ⇄ API](#モバイル--api) | 未着手 | [#7](https://github.com/Hashimoto-Teppei/web-iot-makers-challenge-team-c/issues/7) |
| [危険検知アルゴリズム](#危険検知アルゴリズム) | 未着手 | [#8](https://github.com/Hashimoto-Teppei/web-iot-makers-challenge-team-c/issues/8) |

節が育って読みにくくなったらファイルを分割する。今は骨組みだけなので1ファイルにまとめている。

---

## デバイス ⇄ モバイル（BLE GATT）

デバイスがペリフェラル、モバイルアプリがセントラル。デバイスに蓄積された検知ログ・走行ログを吸い上げる。

**この節が唯一の正本。** Python 側は TypeScript の Zod スキーマを参照できないため、
デバイス担当とモバイル担当はここだけを見て実装する。スキーマを変えたら両実装を更新すること。

仕様は未確定（#5）。

---

## デバイス ⇄ デバイス（メッシュ）

自転車に搭載した Raspberry Pi Zero W 同士が Wi-Fi でメッシュを構成し、位置情報を交換する。
ここで決めたペイロードがそのまま検知アルゴリズムの入力になり、シミュレータが流し込むデータの形も揃える。

仕様は未確定（#6）。

---

## モバイル ⇄ API

**エンドポイントの型はコードが正本。** `apps/web/src/worker/` のルート定義から Hono RPC で型が伝播するため、
ここにリクエスト/レスポンスの構造を書き写さない（必ず実装とずれる）。
この節には型から読み取れないことだけを書く。

Web の画面は API と同じ Worker 内にあるため単一オリジンで、CORS の考慮は不要。
モバイルアプリはネイティブなので CORS の対象外。

仕様は未確定（#7）。

---

## 危険検知アルゴリズム

3つの検知を**別々の担当が並行実装する**ため、これも境界として先に決める。
検知は1つ1ファイルに分け、共通のインターフェースに揃える。4つ目が増えたときの受け口も兼ねる。

インターフェースは未確定（#8）。**これが決まるまで3つの検知（#9 / #10 / #11）は着手できない。**
担当と進捗は各 Issue の assignee を見ること。

**ハードウェアに触れないこと。** GPS の読み取りや LED の点灯を検知の中に書くと、開発機（Windows / macOS）で
テストできなくなる。入力を受けて結果を返す純粋な関数として書き、モックデータで pytest を回せる状態を保つ。
