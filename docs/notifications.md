# 通知の設計

- ステータス: **決定**（#29 で出し分けを決めた。**出力の部品自体は未確定** — #13）
- 最終更新: 2026-08-31（警告の出し分けを決めた。#29）

検知した危険を**どうやって人に伝えるか**を決める。検知アルゴリズムが増えるほど出力の取り合いになるため、
「何を検知するか」（`interfaces/detectors.md`）とは別に、ここで境界を引く。

**判断はスマホ、出力はデバイス**（`adr/0006-decision-layer-on-mobile.md`）。
スマホが `alert` に `warn` を書き、デバイスがそれを出力に割り当てる
（運び方は `interfaces/v2v.md`「デバイスへ渡すもの」）。
**`kind` の識別子の一覧と `lv`（1〜3）の意味は `interfaces/detectors.md` が正本**
（生むのは検知の側）。**ここで決め直さない。**
**このファイルが決めるのは境界と、いつ何を出してよいか。**
**何を、どの出力で、どう出すかは [`notifications/arbitration.md`](./notifications/arbitration.md)**、
運び方は `interfaces/v2v.md` である。

**このファイルには決まったことだけを書く。** 未確定の論点は Issue に置く。

## 境界

### 走行中の通知はデバイス側で完結させる

**走行中にスマホを見る行為は取り締まり対象。** モバイルアプリの画面を走行中に見る前提の通知は設計として不可。
警告は LED・ブザー・ディスプレイなど、**デバイスに載っている出力だけ**で伝わる形にする。

モバイルアプリは走行前後の確認・振り返りに使う。**走行中に鳴らすものではない。**

### 出力を2系統に分ける

向き先が違うものを1つの仕組みで扱うと、優先順位の議論が噛み合わなくなる。**混ぜない。**

| 系統 | 向き先 | 例 | 性質 |
| --- | --- | --- | --- |
| **警告** | 乗っている本人 | 急接近（#9）・急ブレーキ（#10）・対向車（#11）・**一時停止の事前通知**（#27）・後方物体（#26） | 危険が起きたときだけ出る。複数が競合する |
| **表示** | 周囲の人・車 | ウインカー（#24）・ブレーキランプ（#25） | 本人の操作や動作に連動する。競合しない |

**表示は警告の優先順位に関与しない。** ウインカーが点いている間も警告は出るし、逆も同じ。
**このファイルが決めるのは警告の側だけ**で、以降「出力」と書いたら警告の出力を指す。

### 調停はハードウェアに触らない場所に置く

複数の警告が同時に出たときに「結局どれをどう出すか」を決める処理は、
**発火中の警告のリストを受け取って出力の指示を返す関数**として書く
（`src/device/notify.py`。形は [`notifications/arbitration.md`](./notifications/arbitration.md)「調停の関数の形」）。
**これはデバイス側に残る。** 判断はスマホへ移ったが（`adr/0006-decision-layer-on-mobile.md`）、
**出力を持っているのはデバイス**であり、後方物体検知の警告もここで合流する。
スマホから届くのは「何が起きたか」（`warn` の `kind`）までで、
**どの出力に割り当てるかはデバイスが決める。**

LED を直接叩く形で書くと、開発機（Windows / macOS）で確かめられなくなる。
出し分けは**誤ると人が気づけない**部分なので、モックだけでテストできる状態を保つ。

## 出し分けは別ファイル

**どの警告をどの出力で出すか・同時に起きたときの優先順位・出し続ける時間・
16×2 の割り付け・通信断の見せ方・`notify.py` の形は、
[`notifications/arbitration.md`](./notifications/arbitration.md) にある。**
このファイルが持つのは**境界と、いつ何を出してよいか**だけ。

## 走行中と停止中

**デバイスのディスプレイにも「ながら運転」の制約がかかる。**
2024年11月施行の改正道路交通法で、自転車の「ながら運転」は**画像表示用装置に表示された画像の注視**も
対象になっている（6か月以下の懲役または10万円以下の罰金、事故を起こせば1年以下／30万円以下。
2026年4月からは青切符の対象）。条文は携帯電話に限らないため、
**ハンドルに付けた自作ディスプレイが対象外である保証はない。**

| 状態 | 出してよいもの |
| --- | --- |
| **走行中** | **警告と、通信断の表示だけ。**一瞥で終わる記号。**文章を出さない** |
| **停止中** | Worker から来た情報を出してよい（[`notifications/arbitration.md`](./notifications/arbitration.md)「停止中の情報表示」） |

**速度が分からないときは「走行中」として扱う。**
速度を持っているのはスマホだけなので、**走行中かどうかの判定もスマホが行い、
`beat` の `mv` として届く**（`interfaces/v2v.md`「デバイスへ渡すもの」）。
**心拍が途切れている間・測位が出ていない間は、止まっているのか走っているのか分からない。**
ここを既定で「停止中」にすると、**通信が切れた瞬間に走行中の画面へ文章が出る。**
防ごうとしている「ながら運転」をこちらから作り込むことになるので、**迷ったら走行中に倒す。**

具体的には、**`link` が `up` で、かつ直近の `beat` の `mv` が偽のときだけ「停止中」**とする。
`up` でなければ、前に受け取った `mv` が残っていても走行中として扱う。
**判定を2か所に持たない**こと。デバイスは受け取った `mv` に従うだけで、
**速度からの判定をデバイス側に作らない**（そもそも速度が届かない）。

### `mv` のしきい値（スマホ側）

| 遷移 | 条件 |
| --- | --- |
| 停止中 → **走行中** | 速度が **2.0 m/s**（約 7.2 km/h）を上回った**瞬間**（待たない） |
| 走行中 → **停止中** | 速度が **0.5 m/s**（約 1.8 km/h）を **3 秒連続**で下回ったとき |

- **測位が無い間・水平精度が 8m を超える間は「走行中」。** 迷ったら走行中に倒す
- **精度から戻るときも待つ。** 精度が 8m 以下に戻っても、そこから **3 秒**は走行中のままにし、
  そのあとで上の速度の判定を再開する。**即座に戻すと、ビル際で精度が 8m を往復するたびに
  走行中と停止中が入れ替わる**（速度側にヒステリシスを置いた理由と同じ）
- **既定値は仮**（`unverified.md` 47）

**速度の幅と時間の両方にヒステリシスを置く。**
1Hz の測位ではゆらぎが 1〜2 m/s 出るので（`unverified.md` 8 / 38）、
**幅だけでは信号待ちの微動で走行中と停止中を往復する。**
往復すると**下段の情報が出たり消えたりし、それ自体が画面を見させる**
（[`notifications/arbitration.md`](./notifications/arbitration.md)「画面の割り付け（16×2）」）。

## 決めていないこと

**出し分けは決め切った**（[`notifications/arbitration.md`](./notifications/arbitration.md)）。
**残っているのは部品の側だけ**で、決まった時点でそちらの表に割り当てる。

- **出力の部品**（ディスプレイ / ブザー / LED の型番とピン）— #13。
  `hardware.md` が正本。**16×2 の LCD 以外に落ち着いたら「画面の割り付け（16×2）」は書き直す**
  （優先順位・保持時間・鳴らし直しの規則は、どの部品でも成立するので残る）
- **`light` を増やして方向を表すか** — 部品が決まってから（「出力のチャンネル」）
- **停止中に出す情報の中身** — #7（何を集計して見せるか）。ここで決めるのは置き場所だけ
