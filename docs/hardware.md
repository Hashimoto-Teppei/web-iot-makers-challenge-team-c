# ハードウェア構成

- ステータス: **仮**（実物を確認して埋める作業は [#13](https://github.com/Hashimoto-Teppei/web-iot-makers-challenge-team-c/issues/13)）
- 最終更新: 2026-08-24

**1台のラズパイに部品を次々足していく前提**のため、このファイルを**配線の唯一の正本**とする。
実物を見ないと分からなくなるのを防ぐことが目的であり、**接続を変えたらここを更新する**。

配線でなぜこの決まりにしているかは `adr/0003-hardware-wiring.md` に書く。ここには**今どうつながっているか**だけを書く。

表の TODO は「まだ実物を確認できていない」という意味で、**作業としては #13 で管理する**。

## ベース

| 項目 | 内容 |
| --- | --- |
| SBC | Raspberry Pi Zero W |
| OS | TODO |
| ロジック電圧 | **3.3V**（5V の信号を GPIO に入れると壊れる → `adr/0003`） |
| **確保台数** | **未定** — 車車間通信の検証には最低2台必要。ただし**センサーによるローカル検知は1台で成立する**（`adr/0004-v2v-transport.md`）。台数が確定するまでシミュレータ前提で開発する |
| **測位** | **モジュールを載せない。**位置情報はスマホの GNSS から BLE で受け取る（`adr/0004-v2v-transport.md`） |

## 状態の言葉

このファイルの表はすべて同じ言葉を使う。**「予約」は他の人がそのピンを取らないための宣言**であり、
配線が終わっていなくても書く。

| 状態 | 意味 |
| --- | --- |
| 未定 | 使うことは決めたが、部品が決まっていない。ピンはまだ取らない |
| 予約 | 部品とピンが決まった。まだ配線していない |
| 使用中 | 配線して動いている |
| 固定 | 用途が決まっていて動かせない（ピン割り当て表のみ） |
| 空き | 誰も使っていない（ピン割り当て表のみ） |

## 搭載するモジュール

部品を足したら**この表と下のピン割り当て表の両方**に行を足す。片方だけ更新すると必ずずれる。

| モジュール | 型番 | 接続 | ピン / アドレス | 用途 | 状態 |
| --- | --- | --- | --- | --- | --- |
| 表示 | TODO | TODO | — | 危険の即時通知と、停止中の情報表示 | 未定（ディスプレイ or LED、#13） |

**GPS モジュール（GY-GPSV3-NEO6M）は載せないことにした**（`adr/0004-v2v-transport.md`）。
位置はスマホから来る。**配線とピン割り当ては git の履歴に残っている**ので、戻す判断をしたときは
このファイルの1つ前の版を見ること。

**載せると決まったものだけを書く。** 使うかどうか自体を検討している段階のものは、この表に入れず
アイデアの Issue で管理する（ウインカー #24 / ブレーキランプ #25 / 物体検知センサー #26）。

**「通信に依存しないローカル検知を持つ」方針は決まっている**（`adr/0004-v2v-transport.md`）が、
**それを何の部品でやるかは決まっていない。**方針が決まったことを部品が決まったことと混同しないこと。
部品が決まったら #26 からこの表に移す。

## ピン割り当て

**新しい部品をつなぐ前に、必ずこの表で空きを確認する。** 2人が別々に配線すると同じピンを取り合う。
使うことにしたら、実際に配線する前にこの表を埋めて PR を出す（早い者勝ちの予約として機能させる）。

**ピンを予約するのは接続方式が決まってから。** 決まる前に取ると、使わないピンが空かないまま残る。

### 電源・GND

**電源とGNDも取り合いになる。** 部品を足したらここにも行を足す。

| 物理ピン | 内容 | 割り当て | 状態 |
| --- | --- | --- | --- |
| 1, 17 | 3.3V 出力（合計で使える電流が小さい → `adr/0003`） | | 空き |
| 2 | 5V（USB 入力の直結。ここから大電流を取らない） | | 空き |
| 4 | 同上 | | 空き |
| 6 | GND | | 空き |
| 9, 14, 20, 25, 30, 34, 39 | GND | | 空き |

### GPIO

| GPIO | 物理ピン | 既定の機能 | 割り当て | 状態 |
| --- | --- | --- | --- | --- |
| 2 | 3 | I2C1 SDA（基板上でプルアップ済み） | I2C の部品すべて | 固定 |
| 3 | 5 | I2C1 SCL（同上） | I2C の部品すべて | 固定 |
| 4 | 7 | | | 空き |
| 5 | 29 | | | 空き |
| 6 | 31 | | | 空き |
| 7 | 26 | SPI0 CE1 | | 空き |
| 8 | 24 | SPI0 CE0 | | 空き |
| 9 | 21 | SPI0 MISO | | 空き |
| 10 | 19 | SPI0 MOSI | | 空き |
| 11 | 23 | SPI0 SCLK | | 空き |
| 12 | 32 | PWM0 | | 空き |
| 13 | 33 | PWM1 | | 空き |
| 14 | 8 | UART TXD | | 空き（**取る前に下の「UART を使う部品を載せるとき」を読む**） |
| 15 | 10 | UART RXD | | 空き（同上） |
| 16 | 36 | | | 空き |
| 17 | 11 | | | 空き |
| 18 | 12 | PWM0 / PCM | | 空き |
| 19 | 35 | PWM1 / PCM | | 空き |
| 20 | 38 | PCM | | 空き |
| 21 | 40 | PCM | | 空き |
| 22 | 15 | | | 空き |
| 23 | 16 | | | 空き |
| 24 | 18 | | | 空き |
| 25 | 22 | | | 空き |
| 26 | 37 | | | 空き |
| 27 | 13 | | | 空き |

GPIO 0 / 1（物理ピン 27 / 28）は HAT の識別用に予約されているため使わない。

### I2C アドレス

I2C は1本の線に複数の部品をぶら下げられるが、**アドレスが重なると両方動かなくなる**。
部品を選ぶ前にここを見て、重複しないことを確認する。

| アドレス | モジュール | 状態 |
| --- | --- | --- |
| — | まだ無し | |

同じアドレスの部品を2つ使いたい場合は、変更ピンで別アドレスにするか I2C マルチプレクサが要る（未検討）。

### UART を使う部品を載せるとき

**GPIO14/15 は空いているが、ただの空きピンではない。** Zero W では既定で Bluetooth が
性能の良い方の UART（PL011）を使っており、GPIO に出ているのは簡易版（ミニ UART）である。
ここに連続してデータが流れる部品（GPS・レーダー・シリアルのセンサー）を載せると、
**Bluetooth と取り合いになる。**

**BLE はいま測位の唯一の供給元**（`interfaces.md`）なので、以前より痛い。
UART の部品を候補に挙げた時点で `adr/0003` の「UART を使う部品は Bluetooth と競合する」を読み、
**3つの選択肢のどれを採るかを先に決めてからピンを予約すること。**

## 測位はスマホから受け取る

デバイスに GPS モジュールを載せない（`adr/0004-v2v-transport.md`）。
**検知アルゴリズムの前提になるのはスマホ側の測位性能**であり、これは端末と置き場所で変わる。

| 項目 | 見込み | 効いてくるところ |
| --- | --- | --- |
| 更新頻度 | **1Hz**（`FusedLocationProvider` に 1000ms を要求） | 時速 20km で**1秒あたり約 5.5m 進む**。急接近の検知には粗い |
| 水平位置精度 | **端末が `Location.getAccuracy()` でメートルで返す。値をそのまま使う** | 換算も係数も要らない。1通ごとに `hacc` として運ぶ（`interfaces.md`） |
| 置き場所の影響 | **ポケットや鞄の中では大きく劣化する** | **ハンドルにマウントし、画面は消す**運用を前提にする（`unverified.md`） |
| 起動から測位まで | A-GPS があるので通常は数秒 | 電源を入れ直しても待たされない |

**「スマホだから高精度」とは限らない。** 多衛星系（QZSS を含む）と A-GPS で有利な一方、
アンテナは金属筐体の中の小さなもので、外付けパッチアンテナを空に向けた専用受信機より不利になりうる。
**置き場所が精度を左右する**ため、運用の前提として決めておく必要がある。

## 電源

TODO — 走行中の給電方法（モバイルバッテリー等）と想定稼働時間。#13 で確定する。

### 消費電流の見積もり

**部品を足すたびにここへ1行足す。** 稼働時間はバッテリー容量 ÷ 合計消費電流で概算できる。
デモの途中で電池が切れるのが一番まずいので、**実測値が取れたら見積もりを実測に置き換える**。

| 部品 | 見積もり | 実測 | 備考 |
| --- | --- | --- | --- |
| Raspberry Pi Zero W（BLE 常時接続） | TODO | — | **BLE は走行中ずっとつながっている。**アイドル時の値で見積もらない |
| 表示 | TODO | — | 部品が未定（#13） |
| **合計** | TODO | — | |

## 配線図

部品が2〜3個を超えたら、写真か手描きの図をここに貼る。
表だけでは実物のどの線がどこに刺さっているか分からなくなるため。

現状は部品が未確定のため無し。
