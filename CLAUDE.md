# CLAUDE.md

Claude Code 向けの指示。**守るべき制約だけを書く。**
決めた理由は `docs/adr/`、手順は `CONTRIBUTING.md` と `docs/setup.md`、
境界の仕様は `docs/interfaces.md` にある。ここでは繰り返さない。

## 何を作っているか

Web×IoT メイカーズチャレンジ（ハッカソン）チームCのリポジトリ。
お題は「岡山県の課題を Web×IoT で解決する」。**自転車の事故と違反を未然に防ぐ**
デバイスとその周辺システムを作る。背景と全体像は `README.md`。

```
[自転車デバイス] <--BLE 常時接続--> [モバイルアプリ] <--HTTPS--> [Cloudflare Worker]
                                                                画面 + API + D1 + DO
                                                                     ^
                                                                     |
                                                    他の自転車のスマホ（同じ経路）
```

- **デバイス（Raspberry Pi Zero W / Python）**: **後方物体検知**（センサー）と**表示・音**。
  **測位はしない。車車間の判断もしない**（`docs/adr/0006-decision-layer-on-mobile.md`）。
- **モバイルアプリ（Expo）**: 測位（GNSS）、**車車間の3つの検知**、**一時停止の事前通知の判断**、中継。
- **Worker**: 画面と API を兼ねる。収集データの蓄積と活用。

通信を使う検知が3つ（急接近 / 前方急ブレーキ / 見えない曲がり角の対向車）。**これはモバイルで動く。**
通信が死んでもデバイスが黙らないよう、**通信に依存しないローカル検知（デバイスの後方物体検知）を
土台として別に持つ**（`docs/adr/0004-v2v-transport.md` / `0006`）。

## 設計上の制約

- **デバイスに GPS モジュールは載せない。** 測位はスマホが行う（`docs/adr/0004-v2v-transport.md`）。
  経路の呼び名は「メッシュ」ではなく**車車間通信**。
- **BLE で位置を運ばない。** スマホからデバイスへ流れるのは**表示指示と心拍だけ**
  （`docs/adr/0006-decision-layer-on-mobile.md`）。周辺車両を毎秒運ぶと、
  **接続間隔ごとに1通という天井を超えた瞬間に測位ごと止まる。**
- **判断はスマホ、警告の出力はデバイス。** この2つを混ぜない。
  スマホが落ちれば車車間の検知は止まるので、**心拍が途切れたことをデバイスが自分で気づいて
  人に見せる**こと（`docs/interfaces/v2v.md`）。**これが無いと 0006 は成立しない。**
- **走行中の通知はデバイス側（ディスプレイ / LED / ブザー）で完結させる。**
  走行中にスマホを見る行為は取り締まり対象。アプリは走行中も動き続ける（測位と中継）が、
  **その画面は走行前後の確認・振り返り用**。走行中に見る前提の UX は設計として不可。
- **走行中のディスプレイに文章を出さない。** 画像表示用装置の注視も「ながら運転」の対象に
  なりうる。情報の表示は停止中に限る（`docs/notifications.md`）。
- **スマホは画面を消したままハンドルに固定して使う前提**で設計する。ポケットや鞄では
  測位の精度が落ち、検知の前提が崩れる（`docs/hardware.md`）。
- **近傍を保つための状態は Durable Object のメモリに置き、D1 に置かない。**
  位置情報は個人情報であり、消える方が既定であること自体が安全。
  **中継のリクエストの中で D1 に書かない**（リアルタイム経路の遅延に永続化を載せない）。
  走行中でも蓄積は取れるが、DO のアラームか `ctx.waitUntil()` で非同期に流すこと
  （`docs/adr/0005-realtime-transport.md`）。
- Raspberry Pi Zero W はリソースが厳しい。BLE の常時接続・検知・表示制御の負荷配分に注意する。
- ラズパイには CHIRIMEN（Node.js 環境）が入っているが**使わない**。組み込み側は Python で実装する。
  ラズパイ上の Node.js に関する提案は不要。

## 技術スタック

| 領域 | 採用 |
| --- | --- |
| デバイス | Python 3.11 / uv / Ruff / basedpyright / pytest |
| Web + API | React / Vite / Hono / Cloudflare Workers / D1 / Durable Objects / Drizzle / Wrangler |
| モバイル | Expo (SDK 57, React Native) — Android 主 |
| 共通 | pnpm workspaces / Turborepo / Biome / Vitest / Zod |
| バージョン管理 | mise |

**選定理由と構成上の判断は `docs/adr/0001-tech-stack.md` が正本。**
Web と API を1つの Worker にまとめる理由、`packages/` を作らない理由、Hono RPC で型を共有する理由、
`apps/device` を pnpm workspace に含める理由は、すべてそこにある。

- ランタイムのバージョンは **`mise.toml` が唯一の情報源**。`.node-version` / `.python-version` を併置しない。
- **`apps/mobile` の依存は `npx expo install` で入れる。** `pnpm add` で latest を入れると壊れる。
  `apps/mobile` だけ React と TypeScript のバージョンが他とずれるのは正常。**揃えにいかない。**
- 大きなメジャーアップデートをハッカソン期間中に取り込まない。

## リポジトリ構成

```
apps/device/   Python + uv。後方物体検知・表示・BLE ペリフェラル（Raspberry Pi 上で動作）
apps/web/      React + Hono。画面と API を1つの Worker で担う
               src/client/（React） src/worker/（Hono + D1 + DO） src/shared/（共有する型）
apps/mobile/   Expo。測位・車車間の3検知・一時停止の事前通知・中継。Android 主
docs/          設計と手順。一覧は README.md
```

- **`src/client/` と `src/worker/` を混ぜない。** 画面担当と API 担当が同じパッケージを触るため、
  境界が曖昧になるとコンフリクトする。共有したい型は `src/shared/` に置く。
- **`packages/` はまだ作らない。** 共有したいものが出ても、2つ目の利用者が現れるまでは利用者側に置く
  （共有 tsconfig はルートの `tsconfig.base.json`）。
- **Python 側は TypeScript のスキーマを参照できない。** デバイスと他コンポーネントの境界は
  BLE GATT 仕様であり、その正本は `docs/interfaces/ble-gatt.md`。変更時は Python 実装・TypeScript 実装・
  ドキュメントの3つを揃える。
- **検知アルゴリズムは `apps/mobile` に置く**（TypeScript）。`apps/device` に車車間の検知を書かない
  （`docs/adr/0006-decision-layer-on-mobile.md`）。
- **`apps/mobile` は Expo Go では動かない**（BLE のネイティブモジュールが要る）。
  `npx expo run:android` で Development Build を作る。
- Biome / Turborepo / tsconfig のベース設定はルートに置き、各アプリは差分だけを持つ。
  Biome の対象から `apps/device` を除外する。

## コマンド

`pnpm dev` / `lint` / `lint:fix` / `typecheck` / `test` / `build`。
一覧は `README.md`、初回のセットアップ手順は `docs/setup.md`。

**`apps/device` の依存とツールは uv が管理する。** ここで `pnpm add` をしない。
ただし turbo が lint / typecheck / test を横断実行するため **`package.json` は必要**で、
その scripts は `uv run` を呼ぶだけにする（消すと CI から Python が漏れる）。
単体で回すときは下のコマンドを直接使う。

| コマンド | 内容 |
| --- | --- |
| `uv sync` | 依存の同期 |
| `uv run pytest` | テスト（`uv run pytest tests/test_x.py::test_y` で単体指定） |
| `uv run ruff check .` / `uv run ruff format .` | lint / format |
| `uv run basedpyright` | 型チェック |

## Windows / macOS の混在

メンバーの開発機は Windows と macOS が混在する。
**どちらでも同じ手順で開発できることを壊さない**（`docs/adr/0001-tech-stack.md`）。

- **シェル依存のコマンドを書かない。** `package.json` の scripts に `rm -rf`、`cp`、
  `FOO=bar cmd` 形式の環境変数指定を書かない。環境変数が必要なら `cross-env`、
  ファイル操作が必要なら Node のスクリプトにする。
- **パス区切りをハードコードしない。** Node は `path`、Python は `pathlib` を使う。
- **改行コードは LF に統一する**（`.gitattributes` で強制）。
- **環境変数に依存する設計にしない。** Windows では mise が shims 経由で動くため
  `mise.toml` の `[env]` が自動適用されない（必要なら `mise run` 経由で実行する）。
- `apps/device` の BLE（BlueZ）と GPIO は **Linux 専用**で、開発機では動かない
  （→「実機なしで開発する」）。

## 実機なしで開発する（重要）

実機の準備と検証に時間がかかり、実機を持たない開発者も多い。
**実機での動作確認を待たずに実装を進める。** 理論上成立することを根拠に進め、形になってから検証する。
これは好みではなく、**満たさないと開発が止まる要件**（`docs/adr/0002-development-lifecycle.md`）。

- **検知ロジックを入出力から分離する。** 車車間の3検知は `apps/mobile` の TypeScript で、
  **位置情報のモックデータだけで Vitest を回せる構造**にすること。
  デバイス側の後方物体検知も同じで、**センサーの読み取りから分離して pytest で回せる**ようにする。
  **ハードウェアや BLE に依存した検知コードはレビューで差し戻す。**
- **検知アルゴリズムは1つ1ファイル**に分け、共通インターフェース
  （入力: 自車と周辺車両の状態 / 出力: 警告 or なし）に揃える。
  3つを別々の担当が並行実装するための境界であり、4つ目が増えたときの受け口でもある。
- **複数台を模すシミュレータを最初に用意する。** PC 上で仮想ノードを複数起動し、
  位置情報を流し込んで検知が発火するかを確認できるようにする。**置き場所は `apps/mobile` 側**
  （検知がそこに移ったため。`docs/adr/0006-decision-layer-on-mobile.md`）。
  実機が揃ってからシミュレータを作るのでは間に合わない。
- **しきい値（接近速度、減速度、距離）をコードに直書きしない。** 設定として外出しし、実地調整できるようにする。
- **未確認の前提は `docs/unverified.md` に積む。**
  レビューで「実機未確認であること」は指摘しない（方針どおり）。指摘するのは**記録していないこと**だけ。

## ハードウェアを触るとき

1台のラズパイに部品を足し続けるため、配線の衝突と破損を防ぐ（`docs/adr/0003-hardware-wiring.md`）。

- **配線の正本は `docs/hardware.md`。** ピンを使う前に空きを確認し、**配線する前に予約の PR を出す**。
  コードの中の GPIO 番号は正本ではない。
- **GPIO 番号をコードに直書きしない。** しきい値と同じく設定として外に出す。
- **3.3V を超える信号を GPIO に入れない。** 5V の部品はレベル変換か分圧を挟む。
- **GPIO から直接大きな電流を取らない。** LED 1個（抵抗経由）までは直結してよい。
  ブザー・複数の LED・モーターはトランジスタや FET を経由する。
- **走行前に配線を物理的に固定する。** 自転車は振動するため、ジャンパ線のままでは抜ける。

## 段階的に育てる（重要）

**最初から仕様・設計が固まっているものではない。** 部品を順次追加しながら試行錯誤で改良していく。
**仕様変更は異常ではなく通常運転**として扱う。

- **部品追加のたびに大規模な書き換えが発生しない構造にする。** センサーやアクチュエータは
  「後から増える」ことを前提に、個々のデバイスドライバとそれを束ねる側を疎結合に保つ。
  特定のセンサー構成を前提とした密結合な実装は、次の部品追加で必ず壊れる。
- **早すぎる抽象化を避ける。** まだ1種類しかないものに汎用フレームワークを用意するより、
  2〜3個目が出てきた時点で共通化する。動くものを早く作って改良を回す方が優先される。

## 機密情報の扱い（public リポジトリ）

**このリポジトリは public。** コミットした時点で世界に公開され、後から消しても履歴に残る。

- **クライアントに渡る環境変数は機密にできない。** `VITE_*` と `EXPO_PUBLIC_*` はバンドルに
  埋め込まれ、利用者が読める。秘密が必要な処理は必ず API サーバー側に置く。
- **Cloudflare の秘密値は `wrangler secret put` で登録する。** `wrangler.jsonc` に書かない。
  ローカル開発用は `.dev.vars` に置き、これはコミットしない。
- **Cloudflare API トークン / アカウント認証情報をリポジトリに置かない。** CI から使う場合は
  GitHub Secrets を使う。public リポジトリでは fork からの PR に Secrets を渡さないよう、
  ワークフローのトリガに注意する。
- **デバイス側の秘密値**（Wi-Fi の PSK、BLE のペアリング情報、API トークン）もコミットしない。
  実値を含む設定ファイルは `.gitignore` に入れる。
- **実走行の GPS ログをコミットしない。** 位置情報は個人情報であり、自宅や行動パターンが特定できる。
  テスト用のモックデータは合成したものを使う。
- 誤ってコミットした場合は、**まず該当する認証情報を無効化・再発行する。** 履歴の書き換えより先に行う。

## 開発ライフサイクル

手順は `CONTRIBUTING.md`、理由は `docs/adr/0002-development-lifecycle.md`。ここには Claude Code が守るものだけを置く。

- **リポジトリの状態を外から見える形で変えるコマンドは、人が指示したときだけ実行する。**
  対象は `git commit` / `git push` / `gh pr create` / `gh pr merge`。
  差分を作るところで止め、**実行の可否は人が決める**。
  「ついでにコミットしておく」「流れで PR を出す」をしない。
  **リポジトリの設定では止めていない。この記述が歯止めになる。**
- **`main` に直接コミットしない。** ブランチを切って PR 経由でマージする。
  ブランチ保護は設定していないため、**仕組みではなく運用で守る**。
- **ブランチ名は `<種別>/<内容>`**（`feat/v2v-protocol`、`docs/ble-gatt`）。
  **PR タイトルは `<種別>: <何をしたか>`。** squash merge のため、そのまま履歴になる。
  種別は `feat` / `fix` / `docs` / `chore` / `refactor` / `test`。
- **レビューは2段階。** ①`docs/` だけを変える設計 PR で合意 → ②実装 PR。
  小さな変更で①は省いてよいが、**コンポーネントの境界に触れる変更では省かない**。
- **`/code-review` はコミットの前に回す。** 作業中のこまめなコミットのたびに回す必要はない。
  **人に見せる形になったときに1回**でよい。
- **CI を通してからレビューを依頼する。** 整形漏れや型エラーの指摘に人の時間を使わない。
- PR は小さく1つの関心事に絞る。
- **3つの危険検知は担当を分けて並行開発する。** 共通インターフェースを先に決める（`docs/interfaces.md`）。
  **実装先は `apps/mobile`（TypeScript）**であって `apps/device` ではない。
- **タスク管理は GitHub Issues。** 30分以上かかる作業は Issue を立てる。
  1 Issue = 1 PR = 半日〜1日の粒度。PR 本文に `Closes #12` で紐づける。

## 開発が初めてのメンバーが多い（重要）

暗黙の前提で進めない。**説明を省くと手が止まり、止まったことも表に出てこない**（`docs/adr/0002-development-lifecycle.md`）。

- **Issue には必ず「完了条件」を書く。** 「動くようにする」ではなく、自分で確認できる形
  （「`pnpm dev` で画面が出る」「CI が通る」）にする。
- **タスクは着手できる粒度まで分解してから割り当てる。** 分解されていない Issue を渡すと
  着手できずに時間だけが過ぎる。手順まで書けたものには `good first issue` を付ける。
- **レビュー指摘には `[must]` / `[nits]` / `[ask]` を付け、理由を添える。**
  分けないと、初めての人はすべてを必須と受け取って手が止まる。
- コードを書くときも説明するときも、**前提を省略しない。**
  「よくある書き方だから」で流さず、なぜそう書くのかを1行添える。

## ドキュメントを書くとき

どのファイルに何を書くかの一覧は `README.md`。ここには守るルールだけを置く。

- **同じことを2箇所に書かない。** 重複に気づいたら、正本を1つ決めて他は参照に置き換える。
- **決まったことを書き、これからやることは Issue に置く。** 決まっていない論点を
  チェックリストや TODO で残すと Issue と二重管理になり、どちらかが必ず古くなる。
  ドキュメント側には「未確定」と Issue 番号だけを書く。
- **担当者をドキュメントに書かない。** 担当は Issue の assignee が正本。
- 「決定済み / 未確定」を明示する。決まっていないことを憶測で埋めない。
- 実装とずれた古い記述は残さず更新する。過去の決定を書き換えるときは、なぜ変えたかを一行添える。
- **1ファイルが 400 行を超えたら、`##` の節を単位に別ファイルへ切り出す**
  （`docs/adr/` は対象外。決定の記録は分けると全体像が失われる）。
  「読みにくくなったら分ける」という基準は判断を先送りにして肥大化を招いたため、行数で線を引く。
  **数えるのはファイル全体の行数**で、節ごとの行数ではない（節を細かく切るほど基準が発火しなくなるため）。
  **ただし中身が1つの `##` 節だけになったら、400 行を超えていてもそこで止める**
  （`###` まで割ると、相互に依存する仕様が複数ファイルに散り、結局いつも全部開くことになる）。
  **ドキュメントはコードと違い再統合のコストがほぼゼロなので、コードより早く分けてよい。**
