# 開発の進め方

**開発が初めての人を想定して書いています。** わからないことがあれば、調べる前に聞いてください。
このプロジェクトでは「30分詰まったら聞く」を推奨しています。聞くのは早いほど良く、
時間を溶かさないことがチーム全体の速度になります。

---

## 1. 環境をつくる

**手順は [docs/setup.md](./docs/setup.md) にあります。** 何を入れるか、OS ごとのコマンド、
エディタの設定、つまずいたときの対処まで、ひととおりそこにまとまっています。
初回はそちらだけを見てください（手順をここに書き写すと、必ずどちらかが古くなります）。

うまくいかないときは、**エラーメッセージ全文をコピーして** Issue かチームのチャットに貼ってください。
「動きません」だけでは誰も助けられませんが、エラー全文があればだいたい解決します。

## 2. 作業の流れ

```
Issue を選ぶ → 自分にアサイン → ブランチを切る → 実装 → 自動レビュー → コミット → CI を通す → PR → レビュー → マージ
```

### Issue を選ぶ

- [Issues](../../issues) から着手するものを選びます。
- **初めての人は `good first issue` ラベルから**選んでください。手順が細かく書いてあります。
- 選んだら**自分をアサイン**します。誰が何をしているかが見えないと作業がぶつかります。
- **`blocked` が付いた Issue は着手できません。** 先に決めるべきことが残っている状態です。
  本文にどの Issue 待ちかが書いてあります。
- **やることは Issue にあります。** ドキュメントの中に作業リストはありません
  （二重管理になるため、ドキュメントには決まったことだけを書いています）。

### ブランチを切る

`main` では作業しません。`<種別>/<内容>` の形で名前をつけます。

```sh
git switch main
git pull
git switch -c feat/v2v-protocol
```

種別は `feat`（機能追加） / `fix`（不具合修正） / `docs`（ドキュメント） / `chore`（設定など） / `refactor` / `test`。

### 実装する

- **1つの Issue で1つのことだけ**やります。ついでに気になった箇所を直したくなったら、
  別の Issue を立ててください。まとめて直すと、レビューが難しくなって結局遅くなります。
- **完了条件を満たしたら、そこで止めます。** 細かい失敗経路や体裁を先回りして作り込むより、
  **まだ手の付いていない機能に時間を回す**方が全体としては前に進みます。気づいたことは
  `docs/unverified.md` か Issue に積んでください（`docs/adr/0002-development-lifecycle.md`）。
- こまめにコミットして構いません。最終的に1つにまとめられます。
- **人に見せる形になったら、コミットする前に `/code-review` を回します**（→「Claude Code を使う人へ」）。
  作業中のこまめなコミットのたびに回す必要はありません。

### CI を通してから PR を出す

```sh
pnpm lint:fix    # 自動で直せるものは直る
pnpm typecheck
pnpm test
```

ここが通ってから PR を出してください。**機械が見つけられる指摘に、人のレビュー時間を使わないため**です。

### PR を出す

- PR のタイトルはそのまま履歴に残ります。`<種別>: <何をしたか>` で書いてください。
- 本文の「レビューしてほしい点」を埋めると、レビューが速く返ってきます。
  自信がないところを正直に書くのがいちばん効きます。
- まだ途中だけど方向性を見てほしい、というときは **Draft PR** で出してください。
  完成させてから全部やり直すより、ずっと速いです。
- Issue と紐づけるため、本文に `Closes #12` と書きます。マージすると Issue が自動で閉じます。

## 3. レビューについて

### レビューを受ける人へ

- 指摘は**コードに対するもの**で、人に対するものではありません。気にせず直しましょう。
- 指摘の意味がわからなければ聞いてください。**わからないまま直すのがいちばん良くない**です。
- 直したら、返信して再レビューを依頼します。

### レビューをする人へ

- **理由を書きます。** 「ここはこうしてください」ではなく「〜だと〜が起きるので、こうしたい」。
  初めての人にとっては、理由がないと次に活かせません。
- **必須と任意を分けます。** 頭に付けてください。
  - `[must]` 直してほしい（マージの条件）
  - `[nits]` 好みの範囲。直さなくてよい
  - `[ask]` 質問。意図を知りたいだけ
  分けないと、初めての人はすべてを必須だと受け取って手が止まります。
- **良いところも書きます。** 何が良かったのかは、指摘と同じくらい伝わりません。
- **`[must]` は「壊れるもの」に絞ります。** 丁寧さの提案は `[nits]` にしてください
  ——ハッカソンの期間内では、**直す時間そのものが他の機能から借りたもの**です。

### レビュー依頼は自動で飛びます

`.github/CODEOWNERS` に記載があるため、PR を出すとレビュー依頼が自動で送られます。
自分で指定する必要はありません（**自分が出した PR には自分宛の依頼は飛びません**。GitHub の仕様です）。

### Claude Code を使う人へ

**コミット・push・PR は自分で判断してください。** Claude Code は差分を作るところまでで、
**出すかどうかは人が決めます**（`CLAUDE.md` にそう書いてあり、Claude Code は毎回それを読みます）。

出す前に見るのは1つだけです — **「いま出そうとしているものが、自分の意図した変更だけか」。**
関係ないファイルが混ざっていたら、いったん止めて分けてください。

**リポジトリの設定では止めていません。** 確認が出るかどうかは各自の Claude Code の設定しだいで、
**出ないこともあります**。勝手にコミットが進んでいると気づいたら、その場で止めて指摘してください。

**`/code-review` はコミットする前に回してください。** 指摘を直したぶんがそのままコミットに入るので、
1周で終わります。

```sh
# 1. 作業が一段落したら、コミットする前にレビューを回す
/code-review

# 2. 指摘を直す（まだコミットしない）

# 3. コミットする
git commit -m "..."

# 4. PR を出す
gh pr create
```

コミットしてからレビューすると、**指摘を直したコミットが増えるたびにレビューをやり直す**ことになります。
順番が違うだけですが、ここが効きます。作業中のこまめなコミットのたびに回す必要はありません。
**人に見せる形になったときに1回**で十分です。

人の目に入る前に整形漏れ・型エラー・単純なバグを潰すのが目的で、**人のレビューを置き換えるものではありません**。

### 承認とマージ

- **承認1人でマージできます。** ただし承認するのは、**その領域を理解している人**です。
  初めての人同士の承認だけでマージしないでください。
- 初めての人も、学習のためにレビューへ参加して構いません（コメントを書く、質問する）。
- マージは **squash merge** です。リポジトリの設定で他の方法を選べないようにしてあるので、
  表示されたボタンをそのまま押して構いません。
- **マージするとブランチは自動で削除されます。** 手元のブランチは自分で消してください。

```sh
git switch main
git pull
git fetch --prune   # 削除済みのリモートブランチを手元からも消す
git branch -D <消すブランチ名>
```

`-D`（大文字）を使います。squash merge は複数のコミットを1つにまとめ直すため、
Git から見ると**手元のブランチのコミットは main に入っていない**扱いになり、`-d`（小文字）では
`not fully merged` と言われて消せません。**PR がマージ済みであることを確認してから** `-D` を使ってください。

## 4. 困ったとき

| 状況 | どうするか |
| --- | --- |
| 環境構築で詰まった | [docs/setup.md](./docs/setup.md) の「うまくいかないとき」を見る。載っていなければエラー全文を貼って聞く |
| 30分考えてわからない | 聞く。これは推奨されている行動です |
| Issue の内容が理解できない | Issue にコメントする。書いた人の説明不足かもしれません |
| 実装の方針で迷う | Draft PR か「設計の相談」Issue を立てる |
| 壊してしまったかも | まず報告する。`main` は履歴が残っているので元に戻せます |

## 5. 気をつけること

- **このリポジトリは公開されています。** パスワード・API キー・実際の走行で記録した GPS ログは
  絶対にコミットしないでください。詳細は [CLAUDE.md](./CLAUDE.md) の「機密情報の扱い」。
- **実機（ラズパイ）での確認は待たなくて構いません。** これは方針です。
  ただし「実機で確かめていない前提」を置いたときは [docs/unverified.md](./docs/unverified.md) に1行足してください。
- **コンポーネントの境界に関わる変更**（BLE の仕様、API、車車間通信のメッセージ、検知の入出力）は、
  実装の前に「設計の相談」Issue で合意してください。ここは他の人の作業に直結します。

---

もっと詳しい背景や、なぜそう決めたのかは [docs/adr/](./docs/adr/) にあります。
