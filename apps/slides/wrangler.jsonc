{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "team-c-slides",
  "compatibility_date": "2026-09-15",
  // スライドは静的なファイルだけで、サーバー側のコードを持たない。
  // そのため main（Worker のエントリ）を書かず、assets だけの Worker としてデプロイする。
  "assets": {
    "directory": "./dist"
  }
}
