"""判断する層。**センサーによる検知を1つにつき1ファイル**置く（`../../../README.md`）。

**ハードウェアを知らない。** 入力はセンサーの読み値と時刻、出力は警告か `None` で、
**モックの入力だけで pytest が回ること**が要件である
（`../../../../../docs/adr/0002-development-lifecycle.md`）。GPIO を読むのは `../hw/` 側。

**車車間の3検知はここに無い。** あれはスマホ（`apps/mobile` の TypeScript）へ移った
（`../../../../../docs/adr/0006-decision-layer-on-mobile.md`）。
**デバイスに残るのは通信に依存しない検知だけ**である。
"""
