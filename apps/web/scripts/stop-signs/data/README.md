# 一時停止の標識の原本を置く場所

**JARTIC の交通規制情報オープンデータ（CSV）をここに置きます。**
**取得先・ファイルの形・取り込みの手順の正本は
[`../../../README.md`](../../../README.md)「標識を取り込む」**です。
**コマンドはそちらから写してください**——`pnpm --filter web` は `apps/web` を作業ディレクトリに
するので、**ここを起点にしたパスを渡すと開けません。**

## このディレクトリの中身はコミットされません

**このリポジトリは public です。** 原本を置くと**再配布に当たる**ので、
`.gitignore` で止めてあります（`docs/interfaces/stop-signs-source.md`）。
**追跡されるのはこの README だけ**で、`.csv` も、`../out/` に出る `.sql` も入りません。

**`git add --force` で押し込まないでください。** 一度コミットすると履歴に残り、
あとから消しても取り消せません。

## 失うと取り戻せません

**元データは月次更新で、前月ぶんは取得できません。** つまり
**このファイルを失うと、その月のぶんは二度と手に入りません。**

**リポジトリ外へのバックアップは取らないと決めてあります。**
判断とその理由の正本は
[`docs/interfaces/stop-signs-source.md`](../../../../../docs/interfaces/stop-signs-source.md)。

- **新しい月を取り込んでも、前の月のファイルを消さないでください。**
  ユニークキーが月をまたいで安定しているかの確認に要ります（`docs/unverified.md` 63）

## いま置いてあるもの

| ファイル | 取り込み先 |
| --- | --- |
| `岡山県警_202607_k_2.1.csv` | 版 `eb69354e35d28ddb` / 28,651 件（2026-09-01 にリモート D1 へ投入） |

**この表は手で更新します。** 何が入っているかを D1 側だけで持つと、
**原本を持っている人が「どの月を入れたか」を確かめる手段が無くなる**ためです。
